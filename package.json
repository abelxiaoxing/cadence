{
  "name": "@abelxiaoxing/cadence",
  "version": "1.3.0",
  "description": "Four explicit Abel workflow prompts with stage-isolated design and diagnosis, durable resumable implementation, and three package-owned professional Agents.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "keywords": [
    "pi-package",
    "abel",
    "workflow",
    "cadence",
    "openspec"
  ],
  "files": [
    "prompts",
    "skills",
    "config",
    "src",
    "agents",
    "README.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md",
    "licenses"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ],
    "prompts": [
      "./prompts/*.md"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "pack:check": "node scripts/pack-check.mjs",
    "traceability:check": "node scripts/traceability-check.mjs",
    "verify": "bun run check && bun run lint && bun run test && bun run pack:check",
    "check": "node scripts/check-syntax.mjs && bun scripts/build-workspace-io.mjs --check && tsc --noEmit",
    "lint": "biome check . && rumdl check .",
    "test": "vitest run",
    "test:target": "vitest run",
    "check:agents": "node scripts/check-agents.mjs",
    "build:workspace-io": "bun scripts/build-workspace-io.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "@types/node": "^22.15.3",
    "rumdl": "^0.2.10",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abelxiaoxing/cadence.git"
  },
  "homepage": "https://github.com/abelxiaoxing/cadence",
  "bugs": {
    "url": "https://github.com/abelxiaoxing/cadence/issues"
  }
}
