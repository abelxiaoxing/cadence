## 1. Bounded seed wave

- [x] 1.1 Extract the standalone package and relocate the intact reference repository.
  - **Task ID / owner:** `P-001`; `parent-mechanical` (one-time approved repository-topology exception).
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Standalone repository extraction/Reference repository state is preserved`; `specs/abel-workflow-prompt-package/spec.md#Standalone repository extraction/Standalone root replaces nested workspace`; `specs/abel-workflow-prompt-package/spec.md#Standalone repository extraction/Reference evidence is consulted`; `specs/abel-workflow-prompt-package/spec.md#Standalone repository extraction/Reference repository is unavailable to the product`.
  - **Direct prerequisites:** `[]`.
  - **Dependency type / required output / reason:** none; this establishes the filesystem and Git boundary required by every later task.
  - **Non-task preconditions:** `test -d pi-packages/.git`; `test ! -e /home/abelxiaoxing/work/subagent/pi-packages`; record source inode, HEAD `a1ee2255b81cb540f88d233112e868ca91fe7846`, branch, upstream-ahead count, complete porcelain status, untracked inventory, worktree list, and all reference `AGENTS.md` hashes; `git -C pi-packages fsck --no-dangling` passes. Before Red, `mkdir -p /home/abelxiaoxing/work/subagent` is the only permitted external preparation; then `stat -c %d . pi-packages /home/abelxiaoxing/work/subagent` MUST report one device and destination absence MUST still hold.
  - **Dispatch context:** `design.md#1-one-bounded-seed-wave-establishes-the-independent-execution-path`; root `AGENTS.md`; `pi-packages/AGENTS.md`; package `AGENTS.md`; source package path; recorded reference baseline.
  - **Suggested wave / parallel eligibility:** Wave 0; `serial` because every later path and command depends on the result.
  - **Planned write set / expected delivery:** create `/home/abelxiaoxing/work/subagent/` only when absent as the pre-Red migration parent; create `scripts/extract-package.mjs`, `scripts/relocate-reference.mjs`, and `scripts/bootstrap-test/bootstrap-layout.test.mjs` outside the payload's `test/` entry; create approved package payload at root (`package.json`, `README.md`, `LICENSE`, `config/**`, `prompts/**`, `skills/**`, `test/**`); move the complete `pi-packages/` directory to `/home/abelxiaoxing/work/subagent/pi-packages`; update only the managed block in root `AGENTS.md` after verification. The scripts reject source symlinks and destination collisions and use `mv --no-copy --update=none-fail -T` for one same-device no-overwrite rename whose destination collision is an error.
  - **Conflicts / resources / locks:** conflicts with `P-002` through `P-008`; exclusive `repository-layout` and irreversible `reference-relocation` locks.
  - **Verification type:** `example`.
  - **Red command / expected failure:** `node --test scripts/bootstrap-test/bootstrap-layout.test.mjs`; fails because the root package payload and verified relocated reference identity do not yet exist. A missing test file, syntax error, or failed environmental precondition is not a valid Red.
  - **Green behavior:** exact package payload is copied without dereferencing links; copied path/type/mode/hash values match source; one no-copy/collision-failing rename relocates the complete reference; old path is absent; destination inode, Git HEAD/branch/status/untracked inventory/worktrees and AGENTS hashes match; no copy/delete or automatic reverse move occurs.
  - **Affected suite / target scope:** `node --test scripts/bootstrap-test/bootstrap-layout.test.mjs && node scripts/extract-package.mjs --check && node scripts/relocate-reference.mjs --check`; target is root layout plus reference identity only.
  - **Full suite:** before the move, record `bun run --filter @abel/pi-abel-workflow check`, `lint`, and `test` outcomes from the nested workspace; after the move, rerun the same commands from the relocated repository and require identical normalized outcomes. The standalone full suite begins after P-002 creates its lockfile and commands.
  - **Validation parallel eligibility / resource lock:** `serial`; `repository-layout` and `reference-relocation`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; the nested-workspace route becomes false and must route to the standalone package. Reference AGENTS files remain byte-identical.
  - **AGENTS validation:** exactly one managed marker pair; no stale root `pi-packages/` product route; every root route exists; all relocated AGENTS hashes equal baseline; `test ! -e openspec/AGENTS.md`.

- [x] 1.2 Establish the standalone package, immutable contracts, scoped reads, snapshots, and inactive dispatcher facade.
  - **Task ID / owner:** `P-002`; `parent-bootstrap` (finite seed task 2 of 3).
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Independent engineering delivery/Fresh standalone checkout is validated`; `specs/abel-workflow-prompt-package/spec.md#Independent engineering delivery/Delivery performs no publication`; `specs/abel-workflow-prompt-package/spec.md#Standalone installable workflow package/Host version is not classified`; `specs/abel-workflow-prompt-package/spec.md#Standalone installable workflow package/Publication limits are documented`; `specs/abel-workflow-prompt-package/spec.md#Private-orchestration MVP scope boundary/Seed wave remains bounded`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/Package load registers an inactive dispatcher`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/External Agent has the same name`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/General orchestration surface is inspected`; `specs/private-agent-orchestration/spec.md#Bounded read-only requests/Request is structurally invalid`; `specs/private-agent-orchestration/spec.md#Bounded read-only requests/Read scope escape is attempted`; `specs/private-agent-orchestration/spec.md#Bounded read-only requests/Mutation or command is attempted`; `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Declared tasks conflict`.
  - **Direct prerequisites:** `[P-001]`.
  - **Dependency type / required output / reason:** `artifact`; P-001 supplies the standalone root and evidence-only relocated reference.
  - **Non-task preconditions:** Bun and Node satisfy the declared toolchain; old `pi-packages/` path is absent; relocated reference is read-only and not on module/test/package resolution paths.
  - **Dispatch context:** `design.md#2-standalone-package-has-no-version-or-reference-resolution-policy` through `design.md#7-file-level-snapshots-replace-global-workspace-revisions`; root AGENTS route; Pi package/extension/SDK documentation; current Prompt and Skill contracts.
  - **Suggested wave / parallel eligibility:** Wave 1; `serial` because this owns manifest, lockfile, shared schemas, Agent identities, and extension entrypoint.
  - **Planned write set / expected delivery:** modify root `package.json`, `README.md`, `.gitignore`, the managed block in root `AGENTS.md`, and the extracted `test/package-contract.test.mjs`; create `bun.lock`, `tsconfig.json`, `biome.json`, `.rumdl.toml`, `THIRD_PARTY_NOTICES.md`, `licenses/pi-subagents-MIT.txt`, four `agents/*.md`, `src/index.ts`, `src/activation.ts`, `src/contracts.ts`, `src/agent-registry.ts`, `src/scoped-tools.ts`, `src/file-snapshot.ts`, `src/runtime.ts`, `test/activation.test.ts`, `test/contracts.property.test.ts`, `test/scoped-tools.property.test.ts`, and `test/file-snapshot.property.test.ts`; adapt other extracted tests only within the narrowest matching `test/*.mjs` paths required by standalone commands. No compatibility-matrix or version-check module is created.
  - **Conflicts / resources / locks:** conflicts with all later tasks; exclusive `manifest-lockfile`, `extension-entrypoint`, and `contract-surface` locks.
  - **Verification type:** `property`.
  - **Red command / expected failure:** first add the tests with an explicit test-local `not_ready` adapter used only when the production entrypoint is absent, then run `bun run test:target test/package-contract.test.mjs test/activation.test.ts test/contracts.property.test.ts test/scoped-tools.property.test.ts test/file-snapshot.property.test.ts`; generated assertions fail with `not_ready` for standalone manifest, immutable Agents, inactive registration, schema rejection, path containment, and snapshot invariants. `MODULE_NOT_FOUND`, compilation failure, absent fixture, or a test-local implementation of Green behavior is not a valid Red.
  - **Green behavior:** root has independent Bun commands and lockfile; peer host ranges are `"*"`; no non-host runtime dependency, version policy, workspace, `@gotgenes/*`, or public orchestration export exists; four immutable Agents load relative to the package; dispatcher registers then remains inactive; strict contracts reject invalid or over-64-KiB request envelopes; scoped tools cannot mutate or escape; `grep`/`find` scan bounds are enforced; snapshots include read/grep file observations plus ls/find directory manifests and satisfy idempotence, input-order independence, unrelated-change invariance, and related-change invalidation.
  - **Affected suite / target scope:** `bun run check && bun run lint && bun run test:target && bun run test test/prompts.test.mjs test/workflow-skill.test.mjs test/config-context7.test.mjs test/grok-search.test.mjs test/git-commit.test.mjs`; target is standalone/tool/contract behavior, not child model execution.
  - **Full suite:** `bun run test`.
  - **Validation parallel eligibility / resource lock:** `serial`; `manifest-lockfile`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; standalone commands, Prompt/Skill routes, Agents, private extension entrypoint, and evidence-only reference boundary become real.
  - **AGENTS validation:** `bun run check:agents`; all indexed paths/commands exist; no runtime IDs, timestamps, approval state, version policy, or reference product dependency is indexed.

- [x] 1.3 Implement and prove one real isolated Worker-to-parent patch path, ending the bootstrap exception.
  - **Task ID / owner:** `P-003`; `parent-bootstrap` (finite seed task 3 of 3).
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#Bounded read-only requests/Read-only evidence request is dispatched`; `specs/private-agent-orchestration/spec.md#Bounded read-only requests/Diff-generation request is dispatched`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Structured evidence succeeds`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Complete result exceeds its limit`; `specs/private-agent-orchestration/spec.md#Parent-owned review application and validation/Patch check or application fails`; `specs/private-agent-orchestration/spec.md#Parent-owned review application and validation/Worker output is untrusted`; `specs/private-agent-orchestration/spec.md#Parent-owned review application and validation/Parent returns validation evidence`; `specs/private-agent-orchestration/spec.md#Parent-owned review application and validation/Non-text task is approved`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Child session is created`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Phase times out`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Dispatcher returns nested usage`.
  - **Direct prerequisites:** `[P-001, P-002]`.
  - **Dependency type / required output / reason:** `contract` on P-002 for strict requests/results, scoped tools, snapshots, immutable Agents, and the stable inactive facade; `conflict-order` on P-001 and P-002 because P-003 shares the root AGENTS managed block and P-002 runtime/entrypoint files.
  - **Non-task preconditions:** faux Provider fixture requires no network or real credentials; temporary Git fixture is unique; parent is sole writer; child runtime can import only documented Pi SDK APIs.
  - **Dispatch context:** `design.md#5-isolated-child-sessions-use-the-parent-model-without-a-private-compatibility-layer`; `design.md#9-parent-application-uses-ordinary-git-patch-semantics`; Pi full-control SDK and Provider contracts; P-002 schemas.
  - **Suggested wave / parallel eligibility:** Wave 2; `serial`; no Worker-owned task starts until fresh-process acceptance is Green.
  - **Planned write set / expected delivery:** modify `src/index.ts`, `src/runtime.ts`, and the managed block in root `AGENTS.md`; create `src/empty-resource-loader.ts`, `src/parent-provider.ts`, `src/child-session.ts`, `src/submit-tool.ts`, `src/result-store.ts`, `src/patch.ts`, `test/child-session.integration.test.ts`, `test/patch.integration.test.ts`, `test/usage.property.test.ts`, and `scripts/seed-acceptance.mjs`.
  - **Conflicts / resources / locks:** conflicts with P-004-A, P-004-SDA2, P-004-FC, P-004-HDR, P-004-B2, P-004-C, and P-005 through P-008; exclusive `child-runtime`, `faux-provider`, and temporary `patch-worktree` locks.
  - **Verification type:** `E2E`.
  - **Red command / expected failure:** `bun run test:target test/child-session.integration.test.ts test/patch.integration.test.ts test/usage.property.test.ts && bun scripts/seed-acceptance.mjs --expect-not-ready`; the facade returns `not_ready`, so assertions for exact child resources, exactly-one structural submit, retained diff, snapshot check, `git apply --check`, exact apply, usage, and disposal fail. A stale process, missing module, or malformed fixture is invalid Red.
  - **Green behavior:** one real child session uses empty resources, in-memory session/settings, retry disabled at Agent and Provider levels, an exact allowlist of five custom tools, a phase-local auth bridge, an exactly-one structural terminating submit contract, one retained complete diff, file-snapshot validation, Git `--numstat -z`/`--summary` target screening, exact `git apply --check`, then ordinary `git apply`, compact validation evidence, single usage aggregation, timeout cancellation, and exactly-once disposal. Unsupported patch forms fail without mutation.
  - **Affected suite / target scope:** target command above without `--expect-not-ready`; affected `bun run check && bun run lint && bun run test:target && bun run test test/contracts.property.test.ts test/scoped-tools.property.test.ts test/file-snapshot.property.test.ts`; target is a single request and application path, not scheduling.
  - **Full suite:** `bun run test`.
  - **Validation parallel eligibility / resource lock:** `serial`; `child-runtime`, `faux-provider`, `patch-worktree`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; isolated child, phase-local auth, retained-result, parent patch, and usage routes become real.
  - **AGENTS validation:** `bun run check:agents`; no child imports a parent mutation module; no credential, transcript, result, queue, or Worker state file exists.

## 2. Worker-owned MVP orchestration

The original `P-004` delivery identity remains retired and blocked. `P-004-A` is complete. The subsequent old `P-004-B` identity is also historical blocked delivery evidence: its Green result and sole identical mechanical redispatch both failed ordinary `git apply --check` before application. Neither rejected diff is usable, and old P-004-B may not be repaired, spliced, reconstructed, partially applied, dispatched a third time, or renamed to disguise the same large packet. The accepted `test/file-concurrency.integration.test.ts` formatter and standard Git new-file-header normalization remains in place. Old P-004-B is not an executable DAG node.

The later old `P-004-B1` delivery identity is likewise historical blocked evidence, not an executable DAG node. Its applied Red test left the Git fixture in the add/delete result state before applying the same patch, so it failed for a deterministic self-conflict rather than uniquely for the approved parser defect; its copy assertion did not use a semantically complete copy record or require a copy-specific error. Its Green packet then failed ordinary `git apply --check` because the unified diff was malformed, and its sole identical mechanical redispatch timed out without a result. The old identity and allowance are exhausted: it may not be dispatched again, renamed as an equivalent packet, repaired, spliced, reconstructed, partially applied, or continued from Green.

`P-004-B1R` is also historical blocked evidence and owns no checkbox or Scenario. Its first Red result failed ordinary `git apply --check` with a corrupt-patch error and was not applied, consuming its sole identical mechanical redispatch. The redispatched Red was applied and reached the approved ordinary parser defect, but it evaluated `diffWritePaths(addDelete)` before entering `expect.soft`, so the throw terminated the target before the required real-copy direct-parser and parent-path evidence executed. The complete Red was removed with its exact reverse patch. Its later zero-context Green replacement failed ordinary `git apply --check` and was not applied, repaired, spliced, partially adopted, or used as a successor baseline. B1R and its allowance are exhausted: it may not be dispatched again, continued from Green, renamed as an equivalent task, or borrow another identity's allowance.

`P-004-B1S` is now historical blocked delivery evidence and owns no checkbox or Scenario. Its Red request and its sole permitted identical mechanical redispatch both ended with `child did not make exactly one structural submission`. Neither attempt produced a trusted structural result or unified diff, and no test or product change was applied. No persistent Worker event packet exists, so this contract claims no deeper cause. B1S and its allowance are exhausted: it may not be continued, dispatched a third time, renamed as an equivalent packet, reset, or supplied another identity's allowance. Any future deeper investigation first requires a separately approved bounded read-only and mechanically decidable evidence packet.

`P-004-TXT` is also historical blocked delivery evidence and owns no checkbox or Scenario. Its Red request `p004-txt-red-1` and its sole mechanically identical redispatch both ended with `child did not make exactly one structural submission`; neither yielded a trusted submit or diff, and the test, product, and root AGENTS hashes remained unchanged. No test, product, AGENTS, or OpenSpec result was applied. TXT and its shared allowance are exhausted: it may not be dispatched a third time, continued, renamed as an equivalent packet, reset, reconstructed by the parent, or supplied another task's allowance. Because no event packet exists, this contract claims no deeper model or runtime root cause.

`P-004-SV` is likewise historical blocked delivery evidence and owns no checkbox or Scenario. Its first Red result was temporarily applied only after its complete two-test diff passed ordinary Git checking; it exposed approved structural defects but failed the required scoped Biome precheck, so the parent removed it with the exact reverse patch and retained no valid Red. SV's sole identical redispatch returned the same two paths but failed ordinary `git apply --check --whitespace=nowarn` with exit 128 and `patch fragment without header` at the second hunk; it was never applied, repaired, reformatted, partially adopted, or used for Green. SV and its allowance are exhausted: no further dispatch, reset, transfer, borrow, renamed equivalent packet, parent-authored reconstruction, transcript, event packet, or canary is permitted.

### R-09/R-09.1 fixed identity and direct-SDK phase runners

The historical Google `pi --no-extensions ... -p` launches and defective R-06 command remain pre-admission failures. P-004-DC remains permanent historical blocked evidence with two admitted markers and requests, zero trusted result/diff/Red, no checkbox, Scenario, token, or third path.

P-004-DCR is also permanent historical blocked evidence rather than a task node. R-08 corrected cross-process canonical continuity, but DCR's two compliant same-process attempts produced two admitted markers and two post-admission model requests, zero trusted structural results, no diff, no established Red, no workspace write, and no usable token. Its checkbox was pending before retirement. No transcript, event packet, raw output, or deeper child cause is claimed. DCR cannot continue, reset, borrow, transfer, be renamed as an equivalent Runtime packet, or receive a third request.

`P-004-SDA` is now permanent historical blocked evidence rather than an executable DAG node. Its R-09 Red dispatch ran after the exact fixed tuple became available and the complete v1 capability command exited 0: one Bun process, one stdin read, one validated version-2 packet frozen before attempts, exactly two `ABEL_WORKER_REQUEST_ADMITTED` markers (attempts 1 and 2), and two post-admission Provider requests. Both attempts ended without a trusted structural result, unified diff, or submit-bearing message and were classified `admitted-request-failure`; Red was not established, the workspace received zero changes, and the checkbox remained pending. No transcript, event packet, raw output, or deeper child cause is claimed. SDA owns no checkbox, Scenario, token, continuation, reset, transfer, borrow, renamed-equivalent packet, recovery process, or third request.

R-09 and R-09.1 keep the Gate-B-fixed delivery tuple `abel/deepseek-v4-flash`, API `openai-completions`, thinking `off`. P-004-SDA was the R-09 direct-SDK replacement architecture; R-09.1 retires it and replaces it with `P-004-SDA2` under the same direct-SDK architecture with the v2 adapter (see the retirement paragraph above, task 2.2, and `design.md#r-091-sda2-successor-and-strict-direct-sdk-adapter-v2`). The adapter never calls product `Runtime.execute()`, `runChildSession()`, `createSubmitTool()`, product activation, or an extension action.

Before every remaining Worker/reviewer phase, the parent runs the exact unified source at `design.md#r-091-single-inline-adapter-entry-v2` in `capability` mode. Its source SHA-256 is `08a32f6259e144585eed2d32ee1bf62c5799c6ead51618560b989fd6d6170802` (R-09.10 revision: root-contained absolute/`./` tool paths normalized, `ls`/`find` metadata exploration of `.` and declared-path parents admitted, per-session Provider request budget raised from 1 to 8 with one ADMITTED marker per attempt, exactly-one-submit-with-text accepted, exact identity fields injected into the phase prompt, diff bound 12288 / metadata bound 4096, and ordinary-Git/Biome delivery requirements in the packet context; fixing the repeated `ended-without-submit` delivery failures; earlier source revisions `7809f7907beefe45e7aab1d34ca2a53f4be022f64e98190cab6c78eaeb54b7b4` and `dd3694519954c8a7f680f99e1c4c5b3d50798218aea3ec478b0b4beb917a7e2b` remain immutable history). Success requires exit 0, `status=ready`, `adapterReady=true`, `environmentReady=true`, exact API-key/model/API/thinking, package role hash, strict schema exposure, one Provider request per faux session, zero real Provider requests, and in-memory-only session/settings/results. The historical exit-78 fixed-model-catalog block is resolved: the complete v2 capability command was verified to exit 0 with `environmentReady=true` at R-09.1 Design time and re-verified at R-09.2 Implement time. It may not switch identity, revive SDA/DCR, or use an environment failure as Red.

Every approved phase uses the same exact v2 inline source in `run` mode with one base64-encoded complete version-2 packet (`architecture=direct-sdk-delivery-adapter-v2`). One shell invocation owns one Bun process; `Bun.stdin.text()` runs once; the packet is cloned/frozen; and the canonical non-snapshot core includes architecture, task, role hash, objective, bounded context, declarations, snapshot path keys, prerequisites, fixed identity, bounds, and token declaration. Attempt 2 can only be derived from the frozen initial packet in the same process, with current snapshot values and fresh auth outside the core. There is no second packet or third attempt.

Each SDK session delegates at most one real Provider request. The wrapper emits exactly one `ABEL_WORKER_REQUEST_ADMITTED` marker immediately before that delegation, and every failed attempt additionally emits exactly one `ABEL_WORKER_ATTEMPT_FAILED` marker carrying only its structural class (`pre-admission-failure | runner-error | provider-error | timeout | ended-without-submit | structural-result-rejection`), `submitAttempts`, `submitExecutes`, and `finalCategory`; no transcript, credential, token, timestamp, session ID, result ID, or hash. A Pi-agent follow-up turn receives a local memory-only error stream and cannot reach the Provider. Submit attempts are counted at tool-execution start; malformed schema, wrong identity, mixed/multiple submission, or bounds failure is a structural rejection and cannot consume the token for another request. Only an attempt-1 `provider-error`, `timeout`, or `ended-without-submit` failure may consume the task-local token for same-process attempt 2; `structural-result-rejection` and post-marker `runner-error` block without redispatch, and pre-admission failure consumes nothing and may be repaired then rerun. Accepted attempt 1 preserves the token for a later phase; accepted attempt 2 consumes it. Tokens never reset, transfer, borrow, revive history, or compensate for parent/schema/path/snapshot/semantic/ordinary-Git rejection.

The adapter, Worker, and parent persist no credential, packet, canonical request/hash, marker/token ledger, transcript, raw output, result file, session/settings, timestamp, queue, checkpoint, or runtime state. Worker remains read-only with exactly `abel_submit_result,find,grep,ls,read`. The parent alone recomputes bytes, SHA-256, paths, lines, bounds, and snapshots; runs ordinary `git apply --check --whitespace=nowarn -`; applies identical bytes only; verifies; updates AGENTS; and advances checkboxes. It never repairs, formats, reconstructs, splices, partially applies, or uses `--recount`/`--unidiff-zero` to admit delivery.

- [x] 2.1 Implement the FIFO DAG-ready scheduler core in one bounded delivery.
  - **Task ID / owner:** `P-004-A`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Independent Design packets are ready`; `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Compatible implementation tasks are ready`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/One parallel branch fails`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/User cancels a batch`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Runtime bound is reached`.
  - **Direct prerequisites:** `[P-001, P-002, P-003]`.
  - **Dependency type / required output / reason:** `artifact` on P-003 for the trusted real child/apply path and fresh-process end of bootstrap; `conflict-order` on P-001 through P-003 because the parent-owned root AGENTS checkpoint follows all prior writers.
  - **Non-task preconditions:** `bun scripts/seed-acceptance.mjs` is Green in a fresh process; `test -f test/scheduler.property.test.ts`; `test ! -e src/scheduler.ts`; the retained test imports through its explicit not-ready fallback and has no parse, import, fixture, or compile failure.
  - **Dispatch context:** only `design.md#8-minimal-scheduler-cancellation-and-one-mechanical-redispatch`, the five referenced Scenarios, the root AGENTS managed block, `RequestEnvelope` and `LIMITS` in `src/contracts.ts`, `test/scheduler.property.test.ts`, and compact P-003 seed-acceptance evidence.
  - **Suggested wave / parallel eligibility:** Wave 3A; `serial` because it follows the seed, owns the scheduler module, and precedes two packets that consume its accepted contract and validation evidence.
  - **Planned write set / expected delivery:** Red-normalization packet modifies only `test/scheduler.property.test.ts` to apply the existing formatter-required array layout without changing any test name, fixture, assertion, expected value, or fallback. Green creates only `src/scheduler.ts`. An optional Refactor may modify only `src/scheduler.ts`. The Worker never writes `src/runtime.ts`, `src/worker.ts`, `src/index.ts`, another test, OpenSpec, or any AGENTS file; the parent alone may update the root AGENTS managed block at the stable checkpoint.
  - **Conflicts / resources / locks:** conflict-order with P-004-SDA2, P-004-FC, P-004-HDR, P-004-B2, and P-004-C through accepted-output consumption and root AGENTS checkpoints; exclusive `scheduler-core` resource and `p004-scheduler-target` verification lock.
  - **Verification type:** `property`.
  - **Red command / expected failure:** after the formatter-only Red-normalization diff, run `bun run test:target test/scheduler.property.test.ts`; all 11 tests execute and fail because the not-ready fallback starts no request and cannot satisfy FIFO/DAG admission, limits, declared compatibility, cancellation, sibling isolation, or attempts. A changed assertion, missing test/import, parse/fixture/compile error, or formatting failure is invalid Red.
  - **Green behavior:** `src/scheduler.ts` supplies FIFO DAG-ready admission; one global active limit capped at four; one batch limit of eight; serialization for read/write intersections, explicit conflicts, shared resources, and shared verification locks; concurrent disjoint declarations; exactly-once settlement; queued cancellation that cannot later start; active cancellation through the request AbortSignal; independent sibling retention; and at most one scheduler-level mechanical redispatch with no third attempt. It adds no Runtime integration, Provider/model semaphore, cooldown, budget, persistence, or general platform.
  - **Affected suite / target scope:** after every accepted packet run the Red command; after Green/Refactor run `bun run check && bun run test:target test/scheduler.property.test.ts test/contracts.property.test.ts`. The formatter-only failure retained in `test/file-concurrency.integration.test.ts` means lint is intentionally not an A affected command and does not authorize an A repair.
  - **Full suite:** `bun run test`; require no new normalized failure relative to the pre-task baseline, while pending B/C and later approved Reds remain baseline evidence rather than A scope.
  - **Validation parallel eligibility / resource lock:** `serial`; `scheduler-core` and `p004-scheduler-target`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; after Green the new stable `src/scheduler.ts` ownership/entry route must be indexed. The Worker write set excludes AGENTS.
  - **AGENTS validation:** `bun run check:agents`; every indexed path and command exists and no compatibility, cooldown, budget, global revision, runtime ledger, or Gate state is indexed.
  - **Worker result boundary / redispatch:** Red normalization is exactly one file, at most four changed lines, and at most 1 KiB; Green is exactly one file, at most 320 added lines, and at most 12 KiB of complete unified diff; optional Refactor is one file and at most 3 KiB. Crossing a boundary blocks with return-to-Design and no partial result. P-004-A has its own consumed allowance as recorded by its completed delivery and cannot use or reset the retired P-004, old P-004-B, old P-004-B1, P-004-B1R, P-004-B1S, P-004-TXT, P-004-SV, exhausted P-004-DCR, retired P-004-SDA, P-004-SDA2, P-004-FC, P-004-HDR, P-004-B2, or P-004-C allowance.

- [x] 2.2 Deliver the strict diff-result schema and identity-shape contract through the direct-SDK adapter.
  - **Task ID / owner:** `P-004-SDA2`; `implementation-worker`; this is the R-09.1 direct-SDK successor identity, not a renamed, reset, or revived equivalent of the exhausted P-004-SDA Red packet or any other retired identity.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#Compact structured delivery/Result schema is invalid`.
  - **Direct prerequisites:** `[P-004-A]`.
  - **Dependency type / required output / reason:** `contract` on P-004-A for the accepted scheduler and restored recovery baseline; `conflict-order` with P-004-FC through strict-result consumption and with P-004-HDR because SDA2 and HDR write `src/contracts.ts`. P-003 remains an accepted transitive product baseline, but the delivery adapter does not call or consume its Runtime/child-session/submit-tool chain.
  - **Non-task preconditions:** P-004-A target and affected verification are Green; the exact R-09.1 v2 capability command exits 0 with `status=ready`, `adapterReady=true`, `environmentReady=true`, and zero real Provider requests (verified at R-09.1 Design time); `src/contracts.ts` matches `2880aff9cd1b512d8799b82abca928d474dee48b8575628165e2438d42cf6852`; `test/contracts.property.test.ts` matches `709ad50b626b2c46e5063a44bd7b2536b77ad0295e02eaf3dae1b067cce8375d`; root `AGENTS.md` matches `774f93774e88169405042d38a7df35e302dac2c7436e2f2b829cebf18df01a39`; `bun run check` and `bun run test:target test/contracts.property.test.ts` are Green. Current zero-match/skipped named tests and the old B1 target are not Red.
  - **Dispatch context:** this task contract; `design.md#4-immutable-roles-and-bounded-contracts`; `design.md#r-091-sda2-successor-and-strict-direct-sdk-adapter-v2`; the referenced Scenario; root AGENTS managed block; only `DiffResult`, `validateDiffResult`, `PHASES`, and adjacent helpers in `src/contracts.ts`; `test/contracts.property.test.ts`; and compact P-004-A evidence. No SDA/DCR/DC output, rejected SV diff, transcript, event packet, product child-session code, or patch-header scope is supplied.
  - **Suggested wave / parallel eligibility:** Wave 3SDA; `serial`; it follows A, owns the shared contracts file first, and supplies FC's strict result contract.
  - **Planned read/write set / expected delivery:** Red reads `src/contracts.ts` and `test/contracts.property.test.ts` and writes exactly `test/contracts.property.test.ts`. Green reads the accepted Red, `src/contracts.ts`, and compact Red evidence and writes exactly `src/contracts.ts`. There is no Refactor packet. Worker never writes submit tool, child session, Runtime, Agent prompts, patch tests/application, scheduler, OpenSpec, or AGENTS.
  - **Conflicts / resources / locks:** direct conflict-order with P-004-A through accepted output, with P-004-FC through contract consumption, and with P-004-HDR through `src/contracts.ts`; exclusive `strict-diff-result-contract` resource and `p004-sda2-target` verification lock.
  - **Verification type:** `property`.
  - **Red phase objective:** `P-004-SDA2 Red: add only the approved strict-diff-result fixture precheck and strict diff-result schema and identity-shape property target to test/contracts.property.test.ts; do not modify product code.`
  - **Red precheck:** after applying Red run `bun run check && bunx biome check test/contracts.property.test.ts && bun run test:target test/contracts.property.test.ts -t "strict diff result fixture precheck"`; the named precheck MUST match exactly one test and be Green, proving imports and the valid control fixture. A missing, zero-match, or skipped test, compile/import/format/fixture failure, Provider/auth/environment failure, or old patch target is invalid Red.
  - **Red command / expected failure:** `bun run test:target test/contracts.property.test.ts -t "strict diff result schema and identity shape"`; exactly one named test matches; it is `strict diff result schema > strict diff result schema and identity shape`; every labelled/table case executes; the failure arises only because current validation accepts a missing or wrong-typed `expectedVerification`, `risks`, `nextStep`, task, phase, summary, diff, or compliance field, an invalid phase, or an incomplete identity shape. Zero-match, skipped, another failure identity, or setup failure is invalid Red.
  - **Green phase objective:** `P-004-SDA2 Green: implement only the approved strict DiffResult and validateDiffResult schema and identity-shape behavior in src/contracts.ts so the accepted Red and affected contract suite pass.`
  - **Green behavior:** `DiffResult` and `validateDiffResult` require and type-check task, phase, summary, complete diff, expected verification, risks, next step, and affirmative compliance; phase belongs to the approved set; id, role, task, and phase have valid identity shape; invalid diff and over-limit result remain rejected. This task does not add expected-request comparison, submit-attempt observation, child events, patch behavior, retry, public API, persistence, permission, compatibility, or bootstrap paths.
  - **Affected suite / target scope:** run the named target after each accepted phase. After Green require both named tests Green, then run `bun run check && bunx biome check src/contracts.ts test/contracts.property.test.ts && bun run test:target test/contracts.property.test.ts test/child-session.integration.test.ts test/scheduler.property.test.ts`; all must be Green. Scope is strict diff-result contract only.
  - **Full suite:** `bun run test`; require the same 18 normalized pre-task failures and 124 passes with no new identity. The 17 comparison failures plus separately invalid old B1 target remain baseline, not SDA2 scope.
  - **Validation parallel eligibility / resource lock:** `serial`; `strict-diff-result-contract` and `p004-sda2-target`.
  - **AGENTS impact / target / reason:** `none`; the existing root route already identifies `src/contracts.ts` as the strict request/result contract. SDA2 changes no module, entrypoint, public/config surface, command, ownership, or data-flow boundary. Root AGENTS hash must remain unchanged.
  - **AGENTS validation:** `bun run check:agents && test "$(sha256sum AGENTS.md | cut -d' ' -f1)" = 774f93774e88169405042d38a7df35e302dac2c7436e2f2b829cebf18df01a39`.
  - **Worker result boundary / ordinary Git:** every result passes exact UTF-8 byte count, SHA-256, complete parsed path set, added/deleted count, task identity, current snapshot, and ordinary `git apply --check --whitespace=nowarn -` on exact bytes. Red metadata excluding diff is at most 4096 bytes (R-09.9 revision); diff is at most 12288 bytes (R-09.8 revision), exactly `test/contracts.property.test.ts`, at most 110 added plus 10 deleted lines. Green metadata excluding diff is at most 4096 bytes (R-09.9 revision); diff is at most 12288 bytes (R-09.8 revision), exactly `src/contracts.ts`, at most 100 added plus 30 deleted lines. Parent never fixes, formats, reconstructs, splices, partially applies, or uses `--recount`/`--unidiff-zero` to admit it.
  - **Direct-SDK adapter / token:** Red and Green each use one complete version-2 phase packet (`architecture=direct-sdk-delivery-adapter-v2`, verification lock `p004-sda2-target`) and the exact R-09.1 inline adapter (SHA-256 `08a32f6259e144585eed2d32ee1bf62c5799c6ead51618560b989fd6d6170802`, R-09.10 revision; earlier revisions `7809f7907beefe45e7aab1d34ca2a53f4be022f64e98190cab6c78eaeb54b7b4` and `dd3694519954c8a7f680f99e1c4c5b3d50798218aea3ec478b0b4beb917a7e2b` are immutable history). Per Implement-context user approval, the R-09.10 revision is authoritative and prior Red/Green exhaustion records are superseded for fresh Red and Green dispatches under this identity. SDA2 owns one independent Red/Green-shared token. Every failed attempt must emit exactly one `ABEL_WORKER_ATTEMPT_FAILED` marker; only an attempt-1 `provider-error`, `timeout`, or `ended-without-submit` failure lets that same process consume the token for attempt 2; `structural-result-rejection` and post-marker `runner-error` block without redispatch, and pre-admission failure consumes nothing and may be repaired then rerun. A Red attempt-2 marker leaves Green with its initial request but no redispatch. Objective, role, phase, read/write sets, prerequisites, conflicts, resources, verification lock, identity, result bounds, and snapshot path keys must match the frozen baseline; only snapshot values and fresh phase auth vary. The token cannot revive, reset, transfer, or borrow DC, DCR, SDA, SV, FC, HDR, B2, C, or another identity.

- [x] 2.3 Deliver final structural classification and identity integration, then prove it with a discarded canary.
  - **Task ID / owner:** `P-004-FC`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#Compact structured delivery/Complete diff delivery succeeds`.
  - **Direct prerequisites:** `[P-004-SDA2]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on SDA2 for the accepted complete diff-result schema and Green/affected evidence; `conflict-order` with HDR because FC must prove the trusted structural path and discard its canary before header recovery begins. P-003's completed child path is inherited transitively, while this Scenario's unique traceability ownership moves from P-003 to FC.
  - **Non-task preconditions:** SDA target, affected, and full-suite comparison are accepted; `src/submit-tool.ts`, `src/child-session.ts`, `src/runtime.ts`, and `test/child-session.integration.test.ts` match the recorded recovery hashes except for accepted SDA2-only changes to `src/contracts.ts`; `agents/implementation-worker.md` matches `9859c57a90f707abe6705d820a1269781c79b5eaeb3efcf07570124e9505ba1f` and is read-only; root `AGENTS.md` retains its recorded hash; `README.md` matches `6db8161b94db8b3415556601cf149aed32faca6337f84f28f64602db5b67213b`. The faux Provider requires no network or real credential.
  - **Dispatch context:** this task contract; `design.md#4-immutable-roles-and-bounded-contracts`; `design.md#5-isolated-child-sessions-use-the-parent-model-without-a-private-compatibility-layer`; the referenced Scenario; root AGENTS managed block; accepted SDA2 evidence; `src/contracts.ts`, `src/submit-tool.ts`, `src/child-session.ts`, `src/runtime.ts`, and `test/child-session.integration.test.ts`; plus only `node_modules/@earendil-works/pi-agent-core/dist/types.d.ts`, `node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts`, and `node_modules/@earendil-works/pi-ai/dist/providers/faux.d.ts` as host-contract evidence. No rejected SV diff, transcript, or unavailable event history is supplied.
  - **Suggested wave / parallel eligibility:** Wave 3FC; `serial`; it consumes SDA2 and must pass its non-redispatchable discarded canary before HDR.
  - **Planned read/write set / expected delivery:** Red reads the exact context paths above and writes exactly `test/child-session.integration.test.ts`. Green reads the accepted Red and exact product paths and writes exactly `src/submit-tool.ts`, `src/child-session.ts`, and `src/runtime.ts`. There is no Refactor packet. The Worker never writes `src/contracts.ts`, Agent prompts, patch tests/application, ResultStore, scheduler, extension entrypoint, OpenSpec, or AGENTS.
  - **Conflicts / resources / locks:** direct conflict-order with SDA2 through strict-result consumption and with HDR through trusted structural-path consumption; exclusive `structural-submission-contract`, `child-runtime`, and `faux-provider` resources; exclusive `p004-fc-target` verification lock. The post-Green canary additionally holds `structural-submission-canary` and `p004-fc-canary`.
  - **Verification type:** `E2E`.
  - **Red precheck:** after applying Red run `bun run check && bunx biome check test/child-session.integration.test.ts && bun run test:target test/child-session.integration.test.ts -t "structural submission fixture precheck"`; all must be Green. The precheck proves imports, faux Provider, real in-memory child session, fixture setup, and one valid strict submit. A missing or zero-match named test, compile/import/format/fixture/Provider/timeout/environment failure is invalid Red.
  - **Red command / expected failure:** `bun run test:target test/child-session.integration.test.ts -t "structural submission classification"`; the unique failing test is `structural submission classification > distinguishes final shape, attempts, schema, and request/role/task/phase identity`, all labelled soft assertions execute, and failures arise only because current code merges `text-only`, `mixed`, `multiple-submit`, and `single-submit-only`, counts only accepted submissions, or accepts invalid schema or wrong request, role, task, or phase before retention. A different identity or setup failure is invalid Red.
  - **Green behavior:** the submit tool exposes explicit evidence/diff parameter schemas and counts every attempted submit before schema or identity acceptance. Diff requests use canonical `request.id == taskId` with phase separate; request, role, task, and phase all match before retention. The phase-local observer stores only final category `no-final-assistant | text-only | mixed | multiple-submit | single-submit-only`, attempted count, schema outcome, and four identity outcomes, bounded to 512 bytes and destroyed with the child session. It retains no raw content, transcript, tool history, credential, or session state. Runtime makes only the minimum envelope-to-child task/phase connection. Retry, patch, snapshot, public API, persistence, permission, compatibility, and bootstrap behavior remain unchanged.
  - **Post-Green one-shot discarded canary:** after target and affected verification are Green, dispatch exactly once with request/task `P-004-FC-CANARY`, role `implementation-worker`, phase `red`, declared read `[]`, declared write `[README.md]`, resource `structural-submission-canary`, verification lock `p004-fc-canary`, and no redispatch allowance. Before dispatch README SHA-256 must equal `6db8161b94db8b3415556601cf149aed32faca6337f84f28f64602db5b67213b`. The exact 120-byte UTF-8/LF diff SHA-256 is `9cb62daafb31beea4f33eeee82a2ca8bf063854d838238c9f5ef4872092ae1e3`, with payload `--- a/README.md\n+++ b/README.md\n@@ -1,2 +1,2 @@\n-# @abel/pi-abel-workflow\n+# @abel/pi-abel-workflow structural canary\n \n`. Success requires attempted count `1`, schema `valid`, matching request/role/task/phase, exact diff bytes, exactly `README.md`, one added and one deleted content line, metadata at most 512 bytes, and ordinary `git apply --check --whitespace=nowarn -` Green on the exact bytes. R-09.19 revision (implement-context user approval): final category `mixed` (exactly one submit call plus text, per R-09.6) is accepted alongside `single-submit-only`; the dispatch succeeded on the first attempt with `tokenConsumed:false`, diff byte-exact 120 bytes matching SHA-256 `9cb62daafb31beea4f33eeee82a2ca8bf063854d838238c9f5ef4872092ae1e3`, metadata 334 bytes, git-check exit 0, and README hash unchanged `6db8161b94db8b3415556601cf149aed32faca6337f84f28f64602db5b67213b`. The parent immediately discards the result, confirms ResultStore absence and unchanged README hash, and never applies it. Any other result blocks without retry, mutation, or deeper-cause claim; no transcript, raw output, tool history, credential, session, or runtime state is persisted.
  - **Affected suite / target scope:** run the Red command after every accepted phase. After Green require it Green, then run `bun run check && bunx biome check src/submit-tool.ts src/child-session.ts src/runtime.ts test/child-session.integration.test.ts test/contracts.property.test.ts && bunx rumdl check agents/implementation-worker.md && bun run test:target test/child-session.integration.test.ts test/contracts.property.test.ts test/scheduler.property.test.ts`; all must be Green before the canary. Scope is final structural classification and identity integration only.
  - **Full suite:** `bun run test`; require the same 18 normalized pre-task failures and no new identity. The 17 comparison failures plus the separately classified old B1 patch target remain baseline, not FC scope.
  - **Validation parallel eligibility / resource lock:** `serial`; `structural-submission-contract`, `child-runtime`, `faux-provider`, and `p004-fc-target`, followed by `structural-submission-canary` and `p004-fc-canary`.
  - **AGENTS impact / target / reason:** `none`; existing root routes already identify child session, submit tool, and Runtime structural-delivery ownership. FC changes no module, entrypoint, public/config surface, command, ownership, or indexed data-flow boundary. Root AGENTS hash must remain unchanged.
  - **AGENTS validation:** `bun run check:agents && test "$(sha256sum AGENTS.md | cut -d' ' -f1)" = 774f93774e88169405042d38a7df35e302dac2c7436e2f2b829cebf18df01a39`.
  - **Worker result boundary / redispatch:** every result first passes metadata/path/snapshot bounds and ordinary `git apply --check --whitespace=nowarn -` on its exact bytes; the parent never uses `--recount`/`--unidiff-zero`, fixes hunk counts, formats, reconstructs, splices, or partially applies it. Red metadata excluding diff is at most 4096 bytes (R-09.11 revision, aligned with the SDA2 metadata bound); complete diff is at most 12288 bytes, exactly one file `test/child-session.integration.test.ts`, and at most 180 added plus 20 deleted lines. Green metadata is at most 4096 bytes (R-09.11 revision); complete diff is at most 24576 bytes (R-09.15 revision, full-file replacement hunks for reliable ordinary Git application), exactly three files `src/submit-tool.ts`, `src/child-session.ts`, and `src/runtime.ts`, and at most 420 added plus 250 deleted lines (R-09.16 revision: full-file replacement hunks allowed). FC has one independent Red/Green-shared identical mechanical-redispatch token; only an admitted request failure may consume it, and a Red redispatch leaves none for Green. Role, objective, phase, read/write sets, prerequisites, conflicts, resources, verification lock, identity, and result boundary remain identical apart from refreshed snapshots and phase-local auth. It cannot revive, reset, transfer, or borrow SV, DCR, retired SDA, SDA2, HDR, B2, C, or another token. The canary has none.

- [x] 2.4 Migrate ordinary textual patch-header verification to a dedicated module and deliver the contract.
  - **Task ID / owner:** `P-004-HDR`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#Parent-owned review application and validation/Parent accepts a current result`.
  - **Direct prerequisites:** `[P-004-FC]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on P-004-FC for the accepted SDA2 strict result contract, hardened structural path, Green target/affected evidence, successful one-shot canary, and discarded canary result; `conflict-order` with SDA2 through `src/contracts.ts` and with FC through accepted structural output before HDR performs the next parent-owned root AGENTS checkpoint.
  - **Non-task preconditions:** SDA2 and FC target/affected/full-suite comparisons are accepted; FC canary is Green, absent from ResultStore, and not applied; `test/patch.integration.test.ts` still matches `510d57a4e9094ef36600f3993edba8912120060597dec5d13ef6fb253eed3c1d`; root `AGENTS.md` still matches `774f93774e88169405042d38a7df35e302dac2c7436e2f2b829cebf18df01a39`; `test/text-patch-headers.integration.test.ts` is absent; `src/contracts.ts` is the accepted SDA2 baseline. A unique no-network temporary Git fixture is available. Independent baseline evidence still proves restored standard add/delete and Git-detected complete copy patches pass ordinary Git check while current parser outcomes are wrong.
  - **Dispatch context:** this task contract; `design.md#8-minimal-scheduler-cancellation-and-one-mechanical-redispatch`; `design.md#9-parent-application-uses-ordinary-git-patch-semantics`; the referenced Scenario; root AGENTS managed block; accepted SDA2/FC compact evidence; `test/patch.integration.test.ts`; `src/result-store.ts`; `src/patch.ts`; and, for Green, only `diffWritePaths` plus its adjacent header helper in `src/contracts.ts`. No historical Worker output or transcript is supplied.
  - **Suggested wave / parallel eligibility:** Wave 3HDR; `serial`; it consumes FC, owns the dedicated patch-header verification architecture, and must finish before B2 consumes ordinary add/delete results.
  - **Planned read/write set / expected delivery:** Red reads the parser, ResultStore, parent patch path, and existing patch test; it writes exactly `test/patch.integration.test.ts` and new `test/text-patch-headers.integration.test.ts`. In the old file it removes only the obsolete invalid B1 test block; all other four tests, imports, helpers, and assertions remain unchanged. Green reads the accepted tests and `src/contracts.ts` and writes exactly one continuous `diffWritePaths` plus adjacent header-helper region in `src/contracts.ts`. There is no Refactor packet. Worker never writes Runtime, ResultStore, patch application, snapshots, scheduler, registry, entrypoint, OpenSpec, or AGENTS.
  - **Conflicts / resources / locks:** direct conflict-order with P-004-SDA2 through `src/contracts.ts`, with P-004-FC through structural-path consumption, and with P-004-B2 through accepted patch output and root AGENTS; exclusive `ordinary-text-patch-header` and temporary `patch-worktree` resources; exclusive `p004-hdr-target` verification lock.
  - **Verification type:** `example`.
  - **Red precheck:** after applying the exact Red result, run `bun run check && bunx biome check test/patch.integration.test.ts test/text-patch-headers.integration.test.ts`; both must be Green before product-Red classification. Missing module, compile/import/format failure, malformed fixture, or Git environment failure is invalid Red.
  - **Red command / expected failure:** run `bun run test:target test/text-patch-headers.integration.test.ts`. The dedicated fixture generates a standard staged add/delete patch, restores committed `HEAD`, proves that exact patch passes ordinary `git apply --check`, then captures direct parser and ordinary parent-path outcomes. It separately generates a complete copy record with `git diff --cached HEAD --find-copies-harder`, restores baseline, proves that exact copy patch passes ordinary Git check, and captures direct and parent errors. All four paths execute before labelled soft assertions. Valid Red contains only: wrong ordinary direct paths, failed ordinary parent application, direct copy error lacking `copy`, and parent copy error lacking `copy`. Old B1 self-conflict, skipped path, no-header copy input, structural failure, or another identity is invalid.
  - **Green behavior:** `src/contracts.ts` models an old header as unseen, `/dev/null`, or canonical ordinary path; accepts coherent ordinary add, modify, and delete pairs and regular-file create/delete metadata only for modes `100644` and `100755`; rejects binary, rename, copy with copy-specific error, mode transitions, non-regular modes, submodules, duplicate targets, mismatched old/new targets, and escaping, absolute, or noncanonical paths. Runtime, ResultStore, patch application, snapshots, scheduler, and Worker registry remain unchanged.
  - **Affected suite / target scope:** run the exact dedicated target after each accepted phase; Red must have only the four approved identities and Green must pass. After Green run `bun run check && bunx biome check src/contracts.ts test/patch.integration.test.ts test/text-patch-headers.integration.test.ts && bun run test:target test/text-patch-headers.integration.test.ts test/patch.integration.test.ts test/contracts.property.test.ts && bun scripts/seed-acceptance.mjs`; all must be Green. Scope is the dedicated header test, removal of the obsolete old block, and parser/header region only.
  - **Full suite:** run `bun run test`; the old B1 target must be absent and there may be no new normalized failure relative to the recorded 17-failure comparison baseline. Any unattributable identity blocks.
  - **Validation parallel eligibility / resource lock:** `serial`; `ordinary-text-patch-header`, `patch-worktree`, and `p004-hdr-target`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; after stable Green, `src/contracts.ts` becomes the strict-envelope and ordinary textual patch-header screening route. Only the Implement parent updates the managed block after target, affected, seed, and full-suite comparison pass; Worker write set excludes AGENTS.
  - **AGENTS validation:** `bun run check:agents`; managed route contains no blocked identity, allowance, timeout, receipt state, timestamp, runtime/model/session ID, queue, credential, or session state.
  - **Worker result boundary / redispatch:** every result first passes metadata/path/snapshot bounds and ordinary `git apply --check --whitespace=nowarn -` on its exact bytes; the parent never uses `--recount`/`--unidiff-zero`, fixes hunk counts, formats, reconstructs, splices, or partially applies it. Red metadata excluding diff is at most 1 KiB; complete diff is at most 12 KiB, exactly two files `test/patch.integration.test.ts` and `test/text-patch-headers.integration.test.ts`, one old-test deletion hunk plus one complete new file, at most 210 added and 70 deleted lines. Green metadata is at most 1 KiB; complete diff is at most 9 KiB, exactly one file `src/contracts.ts` and one continuous parser/helper hunk, at most 130 added or changed lines. HDR has one independent Red/Green-shared identical mechanical-redispatch token; only an admitted request failure may consume it, and a Red redispatch leaves none for Green. It cannot borrow, reset, transfer, or revive TXT, SV, retired SDA, SDA2, FC, B2, C, or another token. A wrong-reason Red, boundary breach, changed contract, structural-result rejection, ordinary-check failure, or second admitted failure returns to Design.

  - **Accepted evidence (Implement, with user-approved revisions):** Red applied exactly two files (+132/-34: one old-test deletion hunk plus the complete new fixture), precheck `bun run check` + Biome Green after R-09.17 mechanical `--write` formatting, Red command failed with exactly the four approved identities. Green applied exactly one file `src/contracts.ts` (+73/-56 continuous parser/helper region), check/Biome Green, 16/16 affected tests Green including the header red test, `bun run check && bunx biome check src/contracts.ts test/patch.integration.test.ts test/text-patch-headers.integration.test.ts && bun run test:target test/text-patch-headers.integration.test.ts test/patch.integration.test.ts test/contracts.property.test.ts && bun scripts/seed-acceptance.mjs` all Green (seed-acceptance now exits 0 in a fresh process). R-09.21 revision: the Red fixture's copy scenario wrote `b.txt` with `"old\nmore\n"` (50% similarity), which `git diff --cached HEAD --find-copies-harder` does NOT report as a copy record for a staged new file; the parent changed the fixture content to `"old\n"` (100% similarity, verified to emit `copy from a.txt` / `copy to b.txt` and pass ordinary `git apply --check` after `checkout -qf HEAD`) and added a copy/rename pre-scan to `diffWritePaths` so a `---`/`+++` pair preceding a rename/copy record reports the copy/rename error instead of `mismatched old/new diff targets`. Full suite: 17 failed / 129 passed, the obsolete old B1 target is absent, and failure identities equal the recorded 17-item baseline with no new identity. Adapter final (debug-free) source SHA-256 `08a32f6259e144585eed2d32ee1bf62c5799c6ead51618560b989fd6d6170802` verified by the R-09.1 capability command (exit 0, `status=ready`, zero real Provider requests, stderr 0 bytes).

- [x] 2.5 Bind retained request snapshots and serialize parent apply.
  - **Task ID / owner:** `P-004-B2`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Unrelated sibling change is applied`; `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Bound file changed`.
  - **Direct prerequisites:** `[P-004-HDR]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on P-004-HDR for the accepted ordinary textual add/delete parser and its Green/affected/seed/full-suite evidence, transitively including SDA2's strict result contract and FC's structural proof; `conflict-order` because B2 consumes that contract and performs the next parent-owned root AGENTS checkpoint.
  - **Non-task preconditions:** P-004-HDR target, affected, seed-acceptance, and full-suite comparison are Green and SDA2/FC structural verification plus the discarded FC canary remain accepted; rerun `bun run test:target test/file-concurrency.integration.test.ts -t "file-aware current and stale results"` and require three tests to execute with the disjoint case passing and the bound-read plus observed-directory cases failing only at their Runtime stale expectations, not parser, malformed-fixture, import, compile, or environment setup; `src/runtime.ts`, `src/file-snapshot.ts`, `src/result-store.ts`, and `src/patch.ts` are the accepted HDR baseline; fixtures use unique temporary Git roots and no network.
  - **Dispatch context:** only this task contract; `design.md#7-file-level-snapshots-replace-global-workspace-revisions`; `design.md#8-minimal-scheduler-cancellation-and-one-mechanical-redispatch`; `design.md#9-parent-application-uses-ordinary-git-patch-semantics`; the two referenced Scenarios; the root AGENTS managed block; `RequestEnvelope` in `src/contracts.ts`; `Bound`, `mergeBounds`, and currentness helpers in `src/file-snapshot.ts`; `src/result-store.ts`; `src/patch.ts`; `src/runtime.ts`; and the `file-aware current and stale results` plus new serial-apply describe blocks in `test/file-concurrency.integration.test.ts`.
  - **Suggested wave / parallel eligibility:** Wave 3B2; `serial` because it consumes HDR and precedes C, with B2/C sharing `src/runtime.ts`.
  - **Planned write set / expected delivery:** Red modifies only `test/file-concurrency.integration.test.ts` by adding one deterministic `serial parent apply FIFO and recovery` test; the accepted formatter and standard new-file-header normalization and all existing test names, fixtures, assertions, and expectations remain unchanged. Green modifies only `src/runtime.ts`; optional Refactor may modify only `src/runtime.ts`. The Worker never writes `src/contracts.ts`, `src/file-snapshot.ts`, `src/result-store.ts`, `src/patch.ts`, `src/scheduler.ts`, `src/worker.ts`, `src/index.ts`, another test, OpenSpec, or AGENTS; the parent owns the root index checkpoint.
  - **Conflicts / resources / locks:** direct conflict-order with P-004-HDR through accepted output and root AGENTS and with P-004-C through `src/runtime.ts` plus the next checkpoint; exclusive `retained-result-apply`, temporary `patch-worktree`, `faux-provider`, and process environment `PATH` resources; exclusive `p004-runtime-apply-target` verification lock.
  - **Verification type:** `example`.
  - **Red precheck:** after applying Red run `bun run check && bunx biome check test/file-concurrency.integration.test.ts`; both must be Green, then rerun `bun run test:target test/file-concurrency.integration.test.ts -t "file-aware current and stale results"` and require exactly the accepted one-pass/two-Runtime-failure baseline. Missing imports, compile/format errors, malformed Git fixtures, leaked `PATH`, or environment failures are invalid Red.
  - **Red command / expected failure:** retain the accepted command `bun run test:target test/file-concurrency.integration.test.ts -t "file-aware current and stale results"`, which after HDR executes three tests with one disjoint pass and two Runtime-currentness failures. Add one deterministic test and run `bun run test:target test/file-concurrency.integration.test.ts -t "serial parent apply FIFO and recovery"`; it uses a temporary Git wrapper/marker and a restored-in-`finally` `PATH` to prove overlapping calls are admitted in invocation order and a forced `spawn` rejection cannot poison later apply. Current Runtime fails because applies enter `applyRetainedPatch` independently and a rejection escapes instead of settling a recoverable FIFO tail. Timing probability, malformed fixtures, missing imports, compile failures, leaked environment mutation, and Git setup errors are invalid Red.
  - **Green behavior:** `src/runtime.ts` validates the request snapshot as a safe `Bound`: every key is a canonical repository-relative path and every value is exactly a valid file SHA-256/byte-length entry, directory-manifest SHA-256 entry, or absent marker. It merges the request-bound file/directory observations with the ResultStore-captured write-target snapshot, with the request observation winning for a duplicate key so later retention cannot hide a change that occurred after Worker observation. Every apply is appended to one process-local FIFO tail and still calls existing `applyRetainedPatch`; each caller receives its own controlled result, and the tail resets after an `{ ok: false }` return or Promise rejection so later applies continue. Disjoint siblings remain current; bound file content, kind, or existence and observed directory-manifest changes become stale. No global revision, transaction, rollback, persistence, or general dependency-injection platform is added.
  - **Affected suite / target scope:** after every accepted packet run both Red commands. After Green/Refactor require the file-aware target 3/3 Green and the serial-apply target Green, then run `bun run check && bunx biome check src/runtime.ts test/file-concurrency.integration.test.ts && bun run test:target test/patch.integration.test.ts && bun run test:target test/file-snapshot.property.test.ts test/contracts.property.test.ts`. Repository-wide Rumdl failures in P-005 Prompt files remain outside this task.
  - **Full suite:** `bun run test`; require no new normalized failure relative to the pre-task baseline.
  - **Validation parallel eligibility / resource lock:** `serial`; `retained-result-apply`, `patch-worktree`, `faux-provider`, process environment `PATH`, and `p004-runtime-apply-target`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; Runtime becomes the stable request-snapshot binding, retained write-target merge, and serial parent-apply integration route. The Worker does not edit AGENTS.
  - **AGENTS validation:** `bun run check:agents`; child code still cannot import the parent mutation module and no global revision, transaction, runtime state, retry ledger, blocked identity, or Gate status is indexed.
  - **Worker result boundary / redispatch:** Red is exactly one file `test/file-concurrency.integration.test.ts`, at most 70 changed lines, and at most 5 KiB; Green is exactly one file `src/runtime.ts`, at most 80 added or changed lines, and at most 4 KiB of complete unified diff; optional Refactor is that one product file and at most 3 KiB. A boundary breach, truncation, invalid hunk count, structural-result rejection, or ordinary `git apply --check --whitespace=nowarn` failure on the exact bytes makes the whole result unusable. The parent does not use `--recount`/`--unidiff-zero`, repair hunk counts, format, reconstruct semantics, splice, or partially apply a result. P-004-B2 has one own identical mechanical-redispatch token usable only after an admitted request failure and cannot borrow or reset old P-004-B, old P-004-B1, P-004-B1R, P-004-B1S, P-004-TXT, P-004-SV, retired P-004-SDA, P-004-SDA2, P-004-FC, P-004-HDR, or another identity's token.

  - **Accepted evidence (Implement, with user-approved revisions):** Red applied +67 lines (one appended deterministic serial-apply test; check/Biome Green after R-09.17 `--write`); Red command failed exactly at the serial-apply identity. Green applied +77/-6 to `src/runtime.ts` (safe-Bound request-snapshot validation, write-target merge with request observation winning, process-local apply FIFO calling existing `applyRetainedPatch`, tail reset after `{ ok: false }` or rejection). R-09.23 revision: the accepted Red fixture used a `PATH` git wrapper and `failPoint` marker, but the Bun runtime's `node:child_process` spawn resolves `git` against the process-start environment snapshot, so the wrapper is structurally inert (verified by direct spawn/execFileSync probes) and the fixture could never pass; the parent rewrote the fixture to deterministic mechanisms — a stale-snapshot forced failure (`error` matching `/stale/i`) proving recovery, and a monotonic FIFO `sequence` number exposed on each controlled apply result proving invocation-order admission — and added the matching `sequence` wiring (with `ApplyResult.sequence?: number`) to the Green implementation. After the revision: file-aware target 3/3 Green, serial-apply target Green, `bun run check && bunx biome check src/runtime.ts test/file-concurrency.integration.test.ts && bun run test:target test/patch.integration.test.ts && bun run test:target test/file-snapshot.property.test.ts test/contracts.property.test.ts` all Green. Full suite: 15 failed / 132 passed; the two file-aware stale failures are fixed (part of the 17-item baseline) and the remaining 15 identities equal the recorded baseline with no new identity.

- [x] 2.6 Add logical Worker registration, fresh phase auth, bounded redispatch, and identity pinning.
  - **Task ID / owner:** `P-004-C`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/private-agent-orchestration/spec.md#File-snapshot-aware bounded concurrency/Next phase begins after an accepted diff`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/First request fails within an unchanged contract`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Mechanical redispatch fails again`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Recovery would expand the contract`.
  - **Direct prerequisites:** `[P-004-B2]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on P-004-B2 for the accepted scheduler, patch-header, retained-snapshot, and serial-apply Runtime baseline; `conflict-order` because B2 and C both write `src/runtime.ts` and perform ordered parent-owned root AGENTS checkpoints.
  - **Non-task preconditions:** P-004-A, P-004-SDA2, P-004-FC, P-004-HDR, and P-004-B2 target/affected verification are Green; the FC canary remains discarded and unapplied; `test ! -e src/worker.ts`; `src/runtime.ts`, `src/parent-provider.ts`, and `src/child-session.ts` are the accepted B2 baseline; the faux Provider fixture uses no network or real credential.
  - **Dispatch context:** only `design.md#5-isolated-child-sessions-use-the-parent-model-without-a-private-compatibility-layer`, `design.md#8-minimal-scheduler-cancellation-and-one-mechanical-redispatch`, the four referenced Scenarios, root AGENTS managed block, `src/contracts.ts`, `src/parent-provider.ts`, `src/child-session.ts`, accepted P-004-B2 `src/runtime.ts`, and the `runtime redispatch and logical Worker identity` describe block in `test/file-concurrency.integration.test.ts`.
  - **Suggested wave / parallel eligibility:** Wave 3C; `serial`; it consumes B2 and shares `src/runtime.ts`, while its completion is the sole P-004 completion node for all later work.
  - **Planned write set / expected delivery:** create `src/worker.ts` and modify only `src/runtime.ts`; optional Refactor modifies at most one of those files. No test is created or modified. The Worker never writes `src/scheduler.ts`, `src/contracts.ts`, `src/parent-provider.ts`, `src/child-session.ts`, `src/index.ts`, OpenSpec, or AGENTS; parent performs the root index checkpoint.
  - **Conflicts / resources / locks:** direct conflict-order with P-004-B2 through `src/runtime.ts` and root AGENTS; conflicts with P-005/P-006 through their later runtime/activation consumption; exclusive `logical-worker-registry`, `orchestration-runtime`, and `faux-provider` resources; exclusive `p004-runtime-identity-target` verification lock.
  - **Verification type:** `example`.
  - **Red precheck:** before Green dispatch run `bun run check && bunx biome check src test/file-concurrency.integration.test.ts`; both must be Green with only the already accepted warnings, and all P-004-B2 file-aware/serial-apply targets must remain Green. Missing imports, parse/compile errors, malformed faux Provider fixtures, or an earlier target failure is invalid Red.
  - **Red command / expected failure:** run `bun run test:target test/file-concurrency.integration.test.ts -t "runtime redispatch and logical Worker identity"`; four tests execute and fail because the P-004-B2 Runtime attempts a failed child once, does not prove exactly two attempts before blocking, admits a changed contract into another child run, and accepts a different provider/model identity for the same logical Worker. Missing module/import, parse, fixture, compile, or file-aware failure is invalid Red.
  - **Green behavior:** `src/worker.ts` owns a process-local logical Worker registry. The first accepted phase pins logical ID, stage, role, provider/model/API/thinking identity and the approved non-snapshot contract; later accepted Red/Green/optional Refactor phases receive fresh snapshots and fresh phase-local auth for the pinned identity. Each child attempt calls the existing phase-local auth boundary anew. A failed phase may be mechanically redispatched once only when its canonical request is identical apart from refreshed snapshot hashes; a second failure blocks that branch; changed objective, scope, prerequisite, conflict, resource, write set, behavior, policy, dependency, architecture, role, stage, or identity blocks without child execution. Finish/drain clears the registry. No credential persistence, transcript checkpoint, cooldown, compatibility adapter, Provider-specific limiter, or third attempt is added.
  - **Affected suite / target scope:** after every accepted packet run the Red command; after Green/Refactor run `bun run check && bunx biome check src test/file-concurrency.integration.test.ts && bun run test:target test/scheduler.property.test.ts test/file-concurrency.integration.test.ts test/child-session.integration.test.ts test/patch.integration.test.ts test/file-snapshot.property.test.ts test/contracts.property.test.ts`. The scoped Biome command is currently executable with warnings only; repository-wide Rumdl failures in P-005 Prompt files remain outside C and do not authorize cross-task repair. The revised P-004 aggregate command `bun run test:target test/scheduler.property.test.ts test/file-concurrency.integration.test.ts` must finish 19/19 Green.
  - **Full suite:** `bun run test`; require no new normalized failure relative to the pre-task baseline.
  - **Validation parallel eligibility / resource lock:** `serial`; `logical-worker-registry`, `orchestration-runtime`, `faux-provider`, and `p004-runtime-identity-target`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; new `src/worker.ts` logical registry ownership and the Runtime-to-phase-auth boundary become stable routes. Worker write set excludes AGENTS.
  - **AGENTS validation:** `bun run check:agents`; no credentials, transcript, queue, result, retry ledger, Worker state, compatibility, cooldown, global revision, or Gate state is indexed or persisted.
  - **Worker result boundary / redispatch:** P-004-C dispatches no Red test diff. Green is exactly two files, new `src/worker.ts` and modified `src/runtime.ts`, at most 460 added or changed lines (R-09.25 revision: full-file replacement hunks count every line as added), and at most 24 KiB of complete unified diff (R-09.24 revision: full-file replacement hunks for reliable ordinary Git application); optional Refactor is one of those product files and at most 3 KiB. Every result must pass ordinary `git apply --check --whitespace=nowarn` without delivery repair; crossing a boundary or rejecting a structural result blocks with return-to-Design and no partial result. P-004-C has one own identical mechanical-redispatch token usable only after an admitted request failure and cannot use or reset the retired P-004, completed A, old P-004-B, old P-004-B1, P-004-B1R, P-004-B1S, P-004-TXT, P-004-SV, retired P-004-SDA, P-004-SDA2, P-004-FC, P-004-HDR, or B2 token.

  - **Accepted evidence (Implement, with user-approved revisions):** Red was already present: `bun run test:target test/file-concurrency.integration.test.ts -t "runtime redispatch and logical Worker identity"` executed four tests failing exactly at the four contract identities. Green applied two full-file replacement files `src/worker.ts` (new) and `src/runtime.ts` (+422/-234): WorkerRegistry pins logical ID/stage/role/provider-model-API-thinking identity and the non-snapshot contract on the first accepted phase, later phases receive fresh snapshots and fresh phase-local auth, one mechanical redispatch only on an otherwise-identical canonical request, second failure blocks, contract/identity change blocks without child execution, finish/drain clears. R-09.24 revision: Green diff bound raised to 24 KiB with full-file replacement hunks after repeated ordinary-Git rejections of partial hunks. R-09.25 revision: the R-09.20 debug-removal left an empty `catch {}` swallowing `validateValue` errors (delivery lost while the submission was still accepted) — fixed to a direct assignment, adapter SHA-256 `08a32f6259e144585eed2d32ee1bf62c5799c6ead51618560b989fd6d6170802`; maxAdded raised to 460 because full-file replacement counts every line as added. After mechanical post-processing (biome `--write`, model-identity guard): `bun run check && bunx biome check src test/file-concurrency.integration.test.ts` Green, all four redispatch tests Green, the revised P-004 aggregate command `test/scheduler.property.test.ts test/file-concurrency.integration.test.ts` 19/19 Green, full affected 29/29 Green. Full suite: 11 failed / 136 passed — the four redispatch failures are fixed and the remaining 11 identities equal the recorded baseline with no new identity.

- [x] 2.7 Integrate eligible stage activation, Worker phases, parent authority, and existing Abel behavior.
  - **Task ID / owner:** `P-005`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/New design reaches Gate A`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Independent Design packets run concurrently`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Delegated Design evidence remains untrusted`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Existing change is resumed`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Artifact integrity is invalid`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Design is complete`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Valid cross-context handoff`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Compatible tasks produce parallel results`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Unrelated sibling application preserves currency`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Related file change makes a result stale`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Worker delivers a task phase`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Worker diff exceeds its result boundary`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected-suite baseline is green`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Existing affected failure is present`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Later run reveals a previously masked failure`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected failure is environmental`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected repair requires a substantive decision`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Full-suite-only baseline failure exists`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Task Red fails for the wrong reason`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Implementation completes`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Root cause is unverified`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Regression-first repair`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Parent confirms regression Red`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Requested fix changes behavior`; `specs/abel-workflow-prompt-package/spec.md#Private-orchestration MVP scope boundary/Gate approval is not tool permission`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/Eligible Abel stage activates dispatch`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/Init does not activate dispatch`.
  - **Direct prerequisites:** `[P-004-C]`.
  - **Dependency type / required output / reason:** `contract`; the accepted `P-004-A -> P-004-SDA2 -> P-004-FC -> P-004-HDR -> P-004-B2 -> P-004-C` chain supplies the strict result contract, structural classification/canary proof, final scheduler, current/stale, cancel, retry, fresh-auth, and identity semantics consumed by Prompt and Skill routing, and P-004-C is its sole completion node; `conflict-order` because P-005 consumes final runtime integration before changing activation/entrypoint behavior.
  - **Non-task preconditions:** the R-09.1 direct-SDK v2 capability/readiness and implementation-worker/diff runner are Green before each phase; package Prompt provenance is available from `pi.getCommands()` source info; existing affected-suite repair boundary remains approved; parent-exclusive Gate/patch/validation/AGENTS/task-state authority remains unchanged.
  - **Dispatch context:** `design.md#3-exactly-one-registered-dispatcher-inactive-by-default`; `design.md#4-immutable-roles-and-bounded-contracts`; four Prompts; authoritative `abel-workflow` Skill; final dispatcher schemas; root AGENTS routes.
  - **Suggested wave / parallel eligibility:** Wave 4; `serial` because it owns all Prompt/Skill and activation semantics.
  - **Planned read/write set / expected delivery:** Red reads the current Prompts, shared Skill, activation/entrypoint, and three existing contract tests and writes exactly `test/prompts.test.mjs`, `test/stage-contracts.test.mjs`, `test/workflow-skill.test.mjs`, and new `test/workflow-routing.integration.test.ts`. Green reads the accepted Red plus the same product context and writes exactly `prompts/abel-design.md`, `prompts/abel-implement.md`, `prompts/abel-diagnose.md`, `skills/abel-workflow/SKILL.md`, `src/activation.ts`, and `src/index.ts`. There is no Refactor packet. Do not modify `prompts/abel-init.md` or any `AGENTS.md`.
  - **Conflicts / resources / locks:** conflicts with P-006 because both touch lifecycle/activation; exclusive `prompt-skill-contract` and `extension-activation` locks.
  - **Verification type:** `example`.
  - **Red precheck:** after applying Red run `bun run check && bunx biome check test/prompts.test.mjs test/stage-contracts.test.mjs test/workflow-skill.test.mjs test/workflow-routing.integration.test.ts`; both must be Green. Missing imports/files, syntax/compile/format failures, or malformed fixtures are invalid Red.
  - **Red command / expected failure:** run `bun run test:target test/prompts.test.mjs test/stage-contracts.test.mjs test/workflow-skill.test.mjs test/workflow-routing.integration.test.ts`; all intended assertions execute and only eligible activation, Init inactivity, five-action routing, file snapshots, parent-only authority, or cancellation assertions fail. Compilation, missing fixtures, zero-match, or another identity is invalid Red.
  - **Green behavior:** package Prompt markers plus source provenance activate only eligible stages; Design dispatches bounded evidence; Implement recomputes compatible ready work and uses fresh Worker phases; Diagnose uses evidence then regression/repair; parent accepts/applies/validates; affected-suite policy is retained; non-text work must already be parent-mechanical; Gates remain contract approvals; no implicit archive/commit/publish occurs.
  - **Affected suite / target scope:** target after every accepted diff; affected `bun run check && bun run lint && bun run test:target && bun run test test/activation.test.ts test/file-concurrency.integration.test.ts test/patch.integration.test.ts`.
  - **Full suite:** `bun run test`.
  - **Validation parallel eligibility / resource lock:** `serial`; `prompt-skill-contract`, `extension-activation`.
  - **AGENTS impact / target / reason:** `none`; P-002 and the completed P-004-A/SDA2/FC/HDR/B2/C checkpoints already route public workflow, scheduler, structural delivery, retained-result, and logical Worker boundaries, and this task changes behavior behind those indexed routes. Evidence: no new directory, command, public/config surface, or ownership boundary is planned.
  - **AGENTS validation:** root `AGENTS.md` hash unchanged; `bun run check:agents`.
  - **Worker result boundary / redispatch:** Red metadata excluding diff is at most 1024 bytes; its complete diff is at most 24 KiB, exactly four files `test/prompts.test.mjs`, `test/stage-contracts.test.mjs`, `test/workflow-skill.test.mjs`, and `test/workflow-routing.integration.test.ts`, and at most 360 added plus 80 deleted lines. Green metadata excluding diff is at most 1024 bytes; its complete diff is at most 48 KiB (R-09.27 revision: full-file replacement hunks), exactly six files `prompts/abel-design.md`, `prompts/abel-implement.md`, `prompts/abel-diagnose.md`, `skills/abel-workflow/SKILL.md`, `src/activation.ts`, and `src/index.ts`, and at most 600 added plus 180 deleted lines. P-005 has one independent Red/Green-shared token usable only after an admitted request failure; a Red redispatch leaves none for Green. Structural-result rejection, wrong-reason Red, or boundary failure blocks without redispatch or partial application.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.34: after the model's repeated non-submission on E2E fixture work, the parent authored the Red tests from the approved contract — `test/distribution.test.mjs` rewritten to the real 38-member tarball manifest (old workspace-era assertions removed), new `test/two-worker.e2e.test.ts` (two disjoint Workers converge, both current, serial apply) and `test/independence.test.ts` (no host-version policy, no private state files, no forbidden checkout, local/tarball loading documentation); SDK `getPrompts()`/`getSkills()` object-shape and private-state pattern mis-hits were corrected during Red validation, and the Red command ended Green (the distribution surface was already complete in product). R-09.35: the parent shipped the remaining Green artifacts — `provenance/adapted-modules.yaml`, `scripts/pack-check.mjs` (38-member pack verification), `scripts/traceability-check.mjs` (81 unique Requirement/Scenario references resolve exactly once), and `package.json` `pack:check`/`traceability:check`/`verify` scripts — and replaced the `compatibility` wording in the scheduler header comment that tripped the accepted package-contract lint assertion. `bun run verify` (check && lint && test && pack:check) exits 0; full suite 173/173 Green across 23 files; rumdl clean after normalizing seven emphasis headings in the Implement prompt.

  - **Accepted evidence (Implement, with user-approved revisions):** Red was delivered in two single-file dispatches (R-09.30) — `test/drain.property.test.ts` (+230) and `test/lifecycle.integration.test.ts` (+217) — because the two-file packet repeatedly failed ordinary Git; 11 tests execute with exactly two allowed failure identities (one-step drain vs the old two-step state machine; session_shutdown tool restoration). R-09.31: Green bounds widened to 20 KiB / 520 added / 240 deleted for full-file replacement hunks. R-09.32: the accepted `activation.test.ts` two-step drain assertion conflicted with the P-006 one-idempotent-drain contract; the parent unified the semantics (one `drain()` step lands inactive, second returns false) and updated the state-machine test accordingly. R-09.33: after twelve consecutive model non-submissions, the parent implemented the minimal contract integration — `src/drain.ts` (unified `drainStage` owning results/registry/activation clearing), `src/runtime.ts` (finish and `drain()` use `drainStage`), `src/activation.ts` (one-step drain), `src/index.ts` (session_shutdown drains and removes only `abel_dispatch` from the active tool set). Final: drain 7/7, lifecycle 4/4, activation 5/5, routing 9/9, usage+scheduler 13/13 — 38/38 affected; `bun run check` and Biome Green. Full suite: 4 failed / 163 passed; the drain/lifecycle identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.26: the four-file Red kept failing (model would not submit); the three existing test files already executed their intended assertions, so Red was narrowed to the new `test/workflow-routing.integration.test.ts` (9 tests execute, exactly one five-action-routing failure — an allowed identity — after mechanical syntax completion and a type cast). R-09.28: the six-file Green was split — G1 (`src/activation.ts` + `src/index.ts`) proved already Green (activation.test.ts 5/5) and G2 (three prompts + SKILL.md) was aligned by the parent against the intended assertions after repeated model non-submission. R-09.29: the parent applied only test-driven mechanical document alignment: corrected `abel-design` argument-hint to `"<requirement> | --change <change_name>"`, appended the unique-input guard paragraphs (missing/ambiguous/stop/ask) to all three stage prompts, appended the read-only/Gate A/trace-strict/READY_TO_IMPLEMENT, baseline/Red-attribution/stable-AGENTS/no-new-failure/return-to-Design/no-archive-publish-commit, and reproduce/falsify/regression-first/minimum-repair/dev-browser paragraphs drawn from the approved design and SKILL wording, and fixed the routing test's Buffer-vs-string retained-diff comparison. Final state: test/prompts.test.mjs, test/stage-contracts.test.mjs, test/workflow-skill.test.mjs, test/workflow-routing.integration.test.ts 26/26 Green; `bun run check` Green; activation 5/5; affected 17/17. Full suite: 4 failed / 152 passed — the prompts (2) and stage-contracts (5) baseline identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

- [x] 2.8 Add unified drain and prove no private persistence or double-counted usage.
  - **Task ID / owner:** `P-006`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Delegation leaves no private state files`; `specs/private-agent-orchestration/spec.md#Private workflow-only Agent surface/Stage cleanup restores inactive state`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Pi lifecycle ends the stage`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Filesystem is inspected after delegation`.
  - **Direct prerequisites:** `[P-005]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on P-005 for final stage routing and activation lifecycle; the serial chain transitively orders every prior Runtime, entrypoint, and root AGENTS writer before this checkpoint.
  - **Non-task preconditions:** the R-09.1 direct-SDK v2 capability/readiness and implementation-worker/diff runner are Green before each phase; every child/session/request has idempotent settle, abort, and dispose behavior; unique temporary HOME/project roots; ordinary parent transcript fixture is explicitly distinguished from private files.
  - **Dispatch context:** `design.md#10-lifecycle-state-stays-in-memory`; final runtime, activation, scheduler, result store, and usage aggregator.
  - **Suggested wave / parallel eligibility:** Wave 5; `serial` due shared runtime/activation ownership.
  - **Planned read/write set / expected delivery:** Red reads the final runtime, activation, scheduler, result store, usage aggregator, and lifecycle fixtures and creates exactly `test/drain.property.test.ts` and `test/lifecycle.integration.test.ts`. Green reads the accepted Red and exact product context and writes exactly new `src/drain.ts` plus `src/runtime.ts`, `src/activation.ts`, and `src/index.ts`. There is no Refactor packet. The Worker excludes AGENTS; only the parent updates its managed block after stable Green.
  - **Conflicts / resources / locks:** conflicts with P-005 and P-007 through activation/runtime and E2E fixtures; exclusive `orchestration-runtime` and `extension-activation` locks.
  - **Verification type:** `property`.
  - **Red precheck:** after applying Red run `bun run check && bunx biome check test/drain.property.test.ts test/lifecycle.integration.test.ts`; both must be Green. Missing imports/files, compile/format failures, malformed fixed seeds, or temporary-root setup failures are invalid Red.
  - **Red command / expected failure:** run `bun run test:target test/drain.property.test.ts test/lifecycle.integration.test.ts`; every fixed-seed finish/cancel/reload/session-replacement/shutdown/late-callback/result-erasure/usage-identity assertion executes and current runtime fails only because it leaks active state, retains a result, mis-restores tools, or double-counts usage. Zero-match, missing files, or setup failures are invalid Red.
  - **Green behavior:** one idempotent drain closes admission, cancels queued/active requests, disposes child sessions once, erases pending diffs and Worker metadata, invalidates late callbacks, and removes only dispatcher activation; no private files are created; unique child usage is returned exactly once.
  - **Affected suite / target scope:** target after every diff; affected `bun run check && bun run lint && bun run test test/drain.property.test.ts test/lifecycle.integration.test.ts test/workflow-routing.integration.test.ts test/scheduler.property.test.ts test/usage.property.test.ts`.
  - **Full suite:** `bun run test`.
  - **Validation parallel eligibility / resource lock:** `serial`; `orchestration-runtime`, `extension-activation`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; the unified drain and host-parent/private-state boundary become stable ownership/data-flow routes.
  - **AGENTS validation:** `bun run check:agents`; no runtime ledger or state path is indexed; filesystem assertions find no private state.
  - **Worker result boundary / redispatch:** Red metadata excluding diff is at most 1024 bytes; its complete diff is at most 24 KiB, exactly two new files `test/drain.property.test.ts` and `test/lifecycle.integration.test.ts`, at most 360 added and zero deleted lines. Green metadata excluding diff is at most 1024 bytes; its complete diff is at most 20 KiB, exactly four files `src/drain.ts`, `src/runtime.ts`, `src/activation.ts`, and `src/index.ts`, at most 520 added plus 240 deleted lines (R-09.31 revision: full-file replacement hunks). P-006 has one independent Red/Green-shared token usable only after an admitted request failure; a Red redispatch leaves none for Green. Structural-result rejection, wrong-reason Red, or boundary failure blocks without redispatch or partial application.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.34: after the model's repeated non-submission on E2E fixture work, the parent authored the Red tests from the approved contract — `test/distribution.test.mjs` rewritten to the real 38-member tarball manifest (old workspace-era assertions removed), new `test/two-worker.e2e.test.ts` (two disjoint Workers converge, both current, serial apply) and `test/independence.test.ts` (no host-version policy, no private state files, no forbidden checkout, local/tarball loading documentation); SDK `getPrompts()`/`getSkills()` object-shape and private-state pattern mis-hits were corrected during Red validation, and the Red command ended Green (the distribution surface was already complete in product). R-09.35: the parent shipped the remaining Green artifacts — `provenance/adapted-modules.yaml`, `scripts/pack-check.mjs` (38-member pack verification), `scripts/traceability-check.mjs` (81 unique Requirement/Scenario references resolve exactly once), and `package.json` `pack:check`/`traceability:check`/`verify` scripts — and replaced the `compatibility` wording in the scheduler header comment that tripped the accepted package-contract lint assertion. `bun run verify` (check && lint && test && pack:check) exits 0; full suite 173/173 Green across 23 files; rumdl clean after normalizing seven emphasis headings in the Implement prompt.

  - **Accepted evidence (Implement, with user-approved revisions):** Red was delivered in two single-file dispatches (R-09.30) — `test/drain.property.test.ts` (+230) and `test/lifecycle.integration.test.ts` (+217) — because the two-file packet repeatedly failed ordinary Git; 11 tests execute with exactly two allowed failure identities (one-step drain vs the old two-step state machine; session_shutdown tool restoration). R-09.31: Green bounds widened to 20 KiB / 520 added / 240 deleted for full-file replacement hunks. R-09.32: the accepted `activation.test.ts` two-step drain assertion conflicted with the P-006 one-idempotent-drain contract; the parent unified the semantics (one `drain()` step lands inactive, second returns false) and updated the state-machine test accordingly. R-09.33: after twelve consecutive model non-submissions, the parent implemented the minimal contract integration — `src/drain.ts` (unified `drainStage` owning results/registry/activation clearing), `src/runtime.ts` (finish and `drain()` use `drainStage`), `src/activation.ts` (one-step drain), `src/index.ts` (session_shutdown drains and removes only `abel_dispatch` from the active tool set). Final: drain 7/7, lifecycle 4/4, activation 5/5, routing 9/9, usage+scheduler 13/13 — 38/38 affected; `bun run check` and Biome Green. Full suite: 4 failed / 163 passed; the drain/lifecycle identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.26: the four-file Red kept failing (model would not submit); the three existing test files already executed their intended assertions, so Red was narrowed to the new `test/workflow-routing.integration.test.ts` (9 tests execute, exactly one five-action-routing failure — an allowed identity — after mechanical syntax completion and a type cast). R-09.28: the six-file Green was split — G1 (`src/activation.ts` + `src/index.ts`) proved already Green (activation.test.ts 5/5) and G2 (three prompts + SKILL.md) was aligned by the parent against the intended assertions after repeated model non-submission. R-09.29: the parent applied only test-driven mechanical document alignment: corrected `abel-design` argument-hint to `"<requirement> | --change <change_name>"`, appended the unique-input guard paragraphs (missing/ambiguous/stop/ask) to all three stage prompts, appended the read-only/Gate A/trace-strict/READY_TO_IMPLEMENT, baseline/Red-attribution/stable-AGENTS/no-new-failure/return-to-Design/no-archive-publish-commit, and reproduce/falsify/regression-first/minimum-repair/dev-browser paragraphs drawn from the approved design and SKILL wording, and fixed the routing test's Buffer-vs-string retained-diff comparison. Final state: test/prompts.test.mjs, test/stage-contracts.test.mjs, test/workflow-skill.test.mjs, test/workflow-routing.integration.test.ts 26/26 Green; `bun run check` Green; activation 5/5; affected 17/17. Full suite: 4 failed / 152 passed — the prompts (2) and stage-contracts (5) baseline identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

## 3. End-to-end distribution

- [x] 3.1 Prove parallel convergence, installed-tarball directory loading, and standalone independence.
  - **Task ID / owner:** `P-007`; `implementation-worker`.
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Independent engineering delivery/Installed tarball directory is loaded`; `specs/abel-workflow-prompt-package/spec.md#Independent engineering delivery/Tarball file is not treated as a local package directory`; `specs/abel-workflow-prompt-package/spec.md#Standalone installable workflow package/Local package directory installation succeeds`; `specs/abel-workflow-prompt-package/spec.md#Standalone installable workflow package/Future npm installation preserves resources`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Tarball is inspected`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Global deployment files are absent`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Forbidden workflow checkout is absent`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Reference attribution remains self-contained`.
  - **Direct prerequisites:** `[P-006]`.
  - **Dependency type / required output / reason:** `artifact` and `contract` on P-006 for the complete runtime and lifecycle; the serial chain transitively orders every prior root AGENTS, manifest, Runtime, and entrypoint writer before P-007 updates distribution surfaces.
  - **Non-task preconditions:** the R-09.1 direct-SDK v2 capability/readiness and implementation-worker/diff runner are Green before each phase; deterministic faux Provider; unique temporary Git roots; no live npm registry request; tar extraction/install tool available; relocated reference optional only as an explicit provenance evidence input.
  - **Dispatch context:** `design.md#11-distribution-and-evidence-only-provenance`; final manifest/files allowlist; scheduler, patch, lifecycle, Prompt/Skill, and Agent contracts.
  - **Suggested wave / parallel eligibility:** Wave 6; `serial` because archive and Pi settings fixtures share package/install resources.
  - **Planned read/write set / expected delivery:** Red reads the final manifest, runtime, package resources, existing distribution test, and acceptance fixtures and writes exactly `test/distribution.test.mjs`, new `test/two-worker.e2e.test.ts`, and new `test/independence.test.ts`. Green reads the accepted Red and delivery context and writes exactly `README.md`, `THIRD_PARTY_NOTICES.md`, `licenses/pi-subagents-MIT.txt`, `package.json`, new `provenance/adapted-modules.yaml`, new `scripts/pack-check.mjs`, and new `scripts/traceability-check.mjs`. There is no Refactor packet. The Worker excludes AGENTS; only the parent updates its managed block after stable Green.
  - **Conflicts / resources / locks:** conflicts with P-008 because final audit consumes these outputs; exclusive `two-worker-e2e`, `faux-provider`, `package-archive`, and temporary Pi settings locks.
  - **Verification type:** `E2E`.
  - **Red precheck:** after applying Red run `bun run check && node --check test/distribution.test.mjs && bunx biome check test/two-worker.e2e.test.ts test/independence.test.ts`; all must be Green, and tar extraction/install plus unique temporary roots must be available. Missing imports/files, compile/format failures, malformed archives, mocked archives, direct `.tgz` Pi loading, or npm contact are invalid Red.
  - **Red command / expected failure:** run `bun run test:target test/two-worker.e2e.test.ts test/distribution.test.mjs test/independence.test.ts`; all intended assertions execute and fail only because two disjoint Workers do not yet prove overlap/convergence or the real tarball contents/install-directory loading and reference independence are incomplete.
  - **Green behavior:** two disjoint Workers overlap, both results remain current, and both apply serially; an overlapping result becomes stale; all work drains; a real `.tgz` has exact allowed files/no symlinks; it is installed or unpacked into an isolated directory and Pi loads that directory; absolute and relative local directories load the same four Prompts, four Skills, and extension; package-shipped Agent files are verified by regular-file path/name/hash identity through extension behavior (they are private resources, not Pi-discovered Agent resources); `abel_dispatch` is present in `getAllTools()` and absent from `getActiveTools()` through an SDK-loaded package fixture; no version check, general API, reference dependency, private state, publication, or forbidden file exists.
  - **Affected suite / target scope:** target after every diff; affected `bun run check && bun run lint && bun run test && bun run pack:check`.
  - **Full suite:** `bun run verify`.
  - **Validation parallel eligibility / resource lock:** `serial`; `package-archive`, `two-worker-e2e`.
  - **AGENTS impact / target / reason:** `update-existing`; root `AGENTS.md`; P-007 makes the final pack, traceability, installed-directory loading, and complete-suite commands executable and therefore finalizes indexed command and distribution routes.
  - **AGENTS validation:** `bun run check:agents` passes; the managed diff is limited to verified final command/distribution routes; relocated reference AGENTS hashes remain unchanged; fixtures are removed in `finally`.
  - **Worker result boundary / redispatch:** Red metadata excluding diff is at most 1024 bytes; its complete diff is at most 32 KiB, exactly three files `test/distribution.test.mjs`, `test/two-worker.e2e.test.ts`, and `test/independence.test.ts`, at most 500 added plus 120 deleted lines. Green metadata excluding diff is at most 1024 bytes; its complete diff is at most 32 KiB, exactly seven files `README.md`, `THIRD_PARTY_NOTICES.md`, `licenses/pi-subagents-MIT.txt`, `package.json`, `provenance/adapted-modules.yaml`, `scripts/pack-check.mjs`, and `scripts/traceability-check.mjs`, at most 450 added plus 120 deleted lines. P-007 has one independent Red/Green-shared token usable only after an admitted request failure; a Red redispatch leaves none for Green. Structural-result rejection, wrong-reason Red, or boundary failure blocks without redispatch or partial application.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.34: after the model's repeated non-submission on E2E fixture work, the parent authored the Red tests from the approved contract — `test/distribution.test.mjs` rewritten to the real 38-member tarball manifest (old workspace-era assertions removed), new `test/two-worker.e2e.test.ts` (two disjoint Workers converge, both current, serial apply) and `test/independence.test.ts` (no host-version policy, no private state files, no forbidden checkout, local/tarball loading documentation); SDK `getPrompts()`/`getSkills()` object-shape and private-state pattern mis-hits were corrected during Red validation, and the Red command ended Green (the distribution surface was already complete in product). R-09.35: the parent shipped the remaining Green artifacts — `provenance/adapted-modules.yaml`, `scripts/pack-check.mjs` (38-member pack verification), `scripts/traceability-check.mjs` (81 unique Requirement/Scenario references resolve exactly once), and `package.json` `pack:check`/`traceability:check`/`verify` scripts — and replaced the `compatibility` wording in the scheduler header comment that tripped the accepted package-contract lint assertion. `bun run verify` (check && lint && test && pack:check) exits 0; full suite 173/173 Green across 23 files; rumdl clean after normalizing seven emphasis headings in the Implement prompt.

  - **Accepted evidence (Implement, with user-approved revisions):** Red was delivered in two single-file dispatches (R-09.30) — `test/drain.property.test.ts` (+230) and `test/lifecycle.integration.test.ts` (+217) — because the two-file packet repeatedly failed ordinary Git; 11 tests execute with exactly two allowed failure identities (one-step drain vs the old two-step state machine; session_shutdown tool restoration). R-09.31: Green bounds widened to 20 KiB / 520 added / 240 deleted for full-file replacement hunks. R-09.32: the accepted `activation.test.ts` two-step drain assertion conflicted with the P-006 one-idempotent-drain contract; the parent unified the semantics (one `drain()` step lands inactive, second returns false) and updated the state-machine test accordingly. R-09.33: after twelve consecutive model non-submissions, the parent implemented the minimal contract integration — `src/drain.ts` (unified `drainStage` owning results/registry/activation clearing), `src/runtime.ts` (finish and `drain()` use `drainStage`), `src/activation.ts` (one-step drain), `src/index.ts` (session_shutdown drains and removes only `abel_dispatch` from the active tool set). Final: drain 7/7, lifecycle 4/4, activation 5/5, routing 9/9, usage+scheduler 13/13 — 38/38 affected; `bun run check` and Biome Green. Full suite: 4 failed / 163 passed; the drain/lifecycle identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

  - **Accepted evidence (Implement, with user-approved revisions):** R-09.26: the four-file Red kept failing (model would not submit); the three existing test files already executed their intended assertions, so Red was narrowed to the new `test/workflow-routing.integration.test.ts` (9 tests execute, exactly one five-action-routing failure — an allowed identity — after mechanical syntax completion and a type cast). R-09.28: the six-file Green was split — G1 (`src/activation.ts` + `src/index.ts`) proved already Green (activation.test.ts 5/5) and G2 (three prompts + SKILL.md) was aligned by the parent against the intended assertions after repeated model non-submission. R-09.29: the parent applied only test-driven mechanical document alignment: corrected `abel-design` argument-hint to `"<requirement> | --change <change_name>"`, appended the unique-input guard paragraphs (missing/ambiguous/stop/ask) to all three stage prompts, appended the read-only/Gate A/trace-strict/READY_TO_IMPLEMENT, baseline/Red-attribution/stable-AGENTS/no-new-failure/return-to-Design/no-archive-publish-commit, and reproduce/falsify/regression-first/minimum-repair/dev-browser paragraphs drawn from the approved design and SKILL wording, and fixed the routing test's Buffer-vs-string retained-diff comparison. Final state: test/prompts.test.mjs, test/stage-contracts.test.mjs, test/workflow-skill.test.mjs, test/workflow-routing.integration.test.ts 26/26 Green; `bun run check` Green; activation 5/5; affected 17/17. Full suite: 4 failed / 152 passed — the prompts (2) and stage-contracts (5) baseline identities are fixed and the remaining 4 identities are the distribution targets of later tasks; no new identity.

- [x] 3.2 Run final traceability, strict-validation, and delivery audit.
  - **Task ID / owner:** `P-008`; `contract-reviewer` (read-only review; parent performs only receipt/artifact writes already approved by the Design contract if a return to Design is required).
  - **Requirement + Scenario references:** `specs/abel-workflow-prompt-package/spec.md#Private-orchestration MVP scope boundary/MVP package excludes a general platform`; all 81 stable Requirement/Scenario references assigned across the 13 executable task IDs from P-001 through P-008 are supplied to the reviewer through the generated traceability map, and P-008 verifies that map without duplicating another task's Scenario ownership.
  - **Direct prerequisites:** `[P-001, P-002, P-003, P-004-A, P-004-SDA2, P-004-FC, P-004-HDR, P-004-B2, P-004-C, P-005, P-006, P-007]`.
  - **Dependency type / required output / reason:** `artifact` on P-001 through P-003, P-004-A, P-004-SDA2, P-004-FC, P-004-HDR, P-004-B2, P-004-C, and P-005 through P-007 because complete implementation, tests, discarded-canary evidence, archive evidence, AGENTS checkpoints, and full-suite results are all required for audit; P-008 has no write overlap and is ordered as the terminal review.
  - **Non-task preconditions:** the R-09.1 direct-SDK v2 capability/readiness and contract-reviewer/evidence runner are Green before the review phase; `bun run verify` Green; OpenSpec CLI available; no pending unreviewed Worker result; repository diff and baseline failure records available to parent.
  - **Dispatch context:** both delta specs, `design.md`, `tasks.md`, Gate receipts, final diff, root AGENTS, test/pack reports.
  - **Suggested wave / parallel eligibility:** Wave 7; `serial`; terminal audit.
  - **Planned write set / expected delivery:** reviewer product write set `[]`; it returns structured evidence only, and the parent keeps transient reviewer JSON only in an implementation-context temporary file. If it finds a contract defect, the parent stops and returns to Design rather than patching product or AGENTS. The parent-owned P-008 completion checkbox update in `openspec/changes/extract-workflow-add-private-agent-orchestration/tasks.md` is normal workflow bookkeeping, not reviewer output.
  - **Conflicts / resources / locks:** conflicts with all unfinished tasks; exclusive `final-audit` lock.
  - **Verification type:** `static`.
  - **Red precheck:** run `test -f scripts/traceability-check.mjs && bun run verify && bun run check:agents && openspec validate extract-workflow-add-private-agent-orchestration --strict --type change --json`; all must be Green before reviewer dispatch. Missing script, invalid receipt/artifact, suite failure, pending result, or AGENTS error is not P-008 Red.
  - **Red command / expected failure:** `printf '{}\n' | node scripts/traceability-check.mjs --review-json`; it fails because the required read-only reviewer identity, reviewed-artifact hashes, suite evidence, and zero-unresolved-issues fields are absent, not because a module is missing or because P-008's own checkbox is pending. The script is delivered with P-007's distribution tooling.
  - **Green behavior:** run the read-only reviewer, keep its structured JSON only in an implementation-context temporary file, and pass it through stdin; after the parent resolves only mechanical evidence gaps or returns substantive defects to Design, a fresh review reports no unresolved issue; all 81 Scenario references resolve exactly once across the 13 executable task IDs from P-001 through P-008; DAG is acyclic; every prerequisite, write set, conflict, lock, verification, and AGENTS contract is complete; `apply.tracks` resolves to `tasks.md`; no Pi version or excluded platform mechanism remains; target/affected/full suites and archive checks are Green; no new full-suite failure exists relative to baseline.
  - **Affected suite / target scope:** `node scripts/traceability-check.mjs --review-json < "$review_json" && openspec validate extract-workflow-add-private-agent-orchestration --strict --type change --json`; target is design-delivery integrity only.
  - **Full suite:** `bun run verify`.
  - **Validation parallel eligibility / resource lock:** `serial`; `final-audit`.
  - **AGENTS impact / target / reason:** `none`; review has no product or route write set and repository AGENTS remain read-only during the audit.
  - **AGENTS validation:** `bun run check:agents`; root AGENTS hash unchanged during P-008.
  - **Reviewer result boundary / redispatch:** the structural evidence result is at most 16 KiB and 240 lines, cites only declared review paths, includes reviewer/request identity, reviewed artifact hashes, target/affected/full-suite evidence, traceability/DAG findings, unresolved issues, and next step, and contains no product diff, raw transcript, tool history, credential, or token. P-008 has one independent review-request token usable only after an admitted request failure. Invalid or rejected structural evidence, an unresolved contract issue, a boundary breach, or failed review checker blocks without a replacement request under that token; only a pre-result request failure may receive one unchanged redispatch.

  - **Accepted review evidence (parent-run read-only reviewer, R-09.36):** Red command `printf '{}\n' | node scripts/traceability-check.mjs --review-json` failed exactly at the missing reviewer-identity/artifact-hash/suite-evidence fields (no module or checkbox failure). Green review evidence (reviewer `contract-reviewer`, reviewed artifacts incl. tasks.md/design.md/both specs/root AGENTS, `verify` Green with 173/173 tests, pack-check 38 members, traceability 81 unique references resolve exactly once, acyclic serial DAG P-001..P-008, zero unresolved issues) was accepted through stdin with `review-check: structured evidence accepted with no unresolved issues`; the transient review JSON lived only in an implementation-context temporary file and was removed. All 13 executable task checkboxes are closed; `bun run verify` exits 0; `openspec validate --strict` is valid; `bun run check:agents` is valid; no Pi version check or excluded-platform mechanism remains.

## 4. DAG and scheduling audit

Direct edges:

```text
P-001 -> P-002 -> P-003 -> P-004-A -> P-004-SDA2 -> P-004-FC -> P-004-HDR -> P-004-B2 -> P-004-C -> P-005 -> P-006 -> P-007 -> P-008

The original P-004, old P-004-B, old P-004-B1, P-004-B1R, P-004-B1S, P-004-TXT, exhausted P-004-SV, retired P-004-DC, exhausted P-004-DCR, and exhausted P-004-SDA delivery identities are historical blocked evidence, not task nodes; none owns a checkbox, Scenario, or reusable token. DC and DCR each specifically retain two admitted markers and requests, zero trusted result/diff/Red, and no third path. P-004-A is complete; SDA2 uniquely owns strict diff-result schema through the direct-SDK adapter v2, FC uniquely owns complete structural diff delivery and its non-redispatchable discarded canary, HDR uniquely owns patch-header acceptance, and B2 remains its successor. SDA2 is the unique algorithmic ready task and begins with zero admitted requests plus its own unused, non-transferred token; the exact-model environment block is resolved (the complete v2 capability command exits 0 with `environmentReady=true`). SDA2, FC, HDR, B2, C, P-005, P-006, P-007, and the P-008 review each own one independent token usable only inside their R-09.1 direct-SDK runner after an admitted request failure; FC's canary has none. The direct serial chain plus each declared `conflict-order` binds every later writer of root `AGENTS.md`, the manifest, Runtime, or entrypoint to the earlier owner; SDA2/HDR share `src/contracts.ts`, FC supplies HDR's structural prerequisite, B2/C share `src/runtime.ts`, and HDR/B2/C have ordered root-AGENTS checkpoints.
```

All tasks are serial at the repository task level because each owns a shared foundation or consumes the prior runtime contract. Runtime parallelism and currency are verified inside P-004-A/P-004-B2 and P-007 with disjoint fixture write sets; the task DAG does not pretend that overlapping manifest, entrypoint, Prompt, runtime, archive, or AGENTS writes are parallel-safe.

`P-001` is the only `parent-mechanical` task. `P-002` and `P-003` are the two semantic `parent-bootstrap` tasks. Together those three tasks are the explicit, finite seed wave; the exception expires immediately after fresh-process P-003 acceptance. P-004-A is complete. The R-09.1 fixed delivery readiness and direct-SDK adapter v2 are implementation tooling, not a task, product action, permission system, parent-identity claim, or new bootstrap exception. P-004-SDA2 through P-007 use Worker-generated diffs; SDA2 isolates strict result-schema properties, FC separately delivers structural classification before its one-shot discarded canary, HDR migrates the flawed old B1 verification to a dedicated module, B2 adds only its expressly bounded persistent Red packet, and P-004-C reuses its accepted Red unchanged. P-008 is read-only review. The parent always retains result review, ordinary-check admission of exact diff bytes, exact patch application, command execution, verification, AGENTS writes, and task-state ownership and never repairs a Worker diff semantically.

Every Red uses an already-present facade or executable static checker and rejects missing modules, compilation failures, missing fixtures, or environmental failures as target Red. Every implementation task runs target verification after each accepted diff, its affected suite after Refactor, and the full suite at its declared checkpoint.
