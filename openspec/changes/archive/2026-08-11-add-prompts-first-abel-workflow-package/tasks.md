## 1. Package and workflow resources

- [x] 1.1 Establish the package boundary, monorepo registration, and AGENTS routes.
  - **Task ID / dependencies:** `1.1`; no dependencies.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Independently installable workflow package/Unsupported older Pi version`; `specs/abel-workflow-prompt-package/spec.md#Independently installable workflow package/Independent git installation is not promised`; `specs/abel-workflow-prompt-package/spec.md#First-phase scope boundary and roadmap/Roadmap is read`.
  - **Verification type:** `static`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/package-contract.test.mjs`.
  - **Expected Red:** The package manifest, metadata, documentation, local registration, release entries, lockfile resolution, roadmap, and AGENTS routes do not yet exist, so the package-contract assertions fail for those missing surfaces; a missing test file or "no test files" result is not the contractual Red and must be corrected before implementation proceeds.
  - **Green behavior:** `@abel/pi-abel-workflow` exists at `packages/pi-abel-workflow/` with Pi `>=0.84.1`, Node.js `>=22`, the approved scripts and development dependencies, MIT license, supported npm/local installation documentation, explicit monorepo-git limitation, five-item non-runnable roadmap, local Pi registration, release-please component, `0.0.0` manifest entry, Bun lockfile resolution, and no publish action.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test && bun run lint`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/package.json`; `pi-packages/packages/pi-abel-workflow/README.md`; `pi-packages/packages/pi-abel-workflow/LICENSE`; `pi-packages/packages/pi-abel-workflow/test/package-contract.test.mjs`; `pi-packages/.pi/settings.json`; `pi-packages/README.md`; `pi-packages/release-please-config.json`; `pi-packages/.release-please-manifest.json`; `pi-packages/bun.lock`; `pi-packages/AGENTS.md`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/AGENTS.md`; add the prompts-first package route and an index-level exception to the legacy all-extension generalization while preserving all human-authored text outside the managed block. `create-index` for `pi-packages/packages/pi-abel-workflow/AGENTS.md`; add only a managed package index for responsibility, resources, tests, and validation routing. The outer `/home/abelxiaoxing/work/pi-abelpackages/AGENTS.md` is `none` because it already routes Bun workspace implementation to `pi-packages/AGENTS.md`. `openspec/AGENTS.md` remains forbidden.
  - **AGENTS validation:** `git diff --check -- AGENTS.md packages/pi-abel-workflow/AGENTS.md && test "$(grep -c '<!-- ABEL:AGENTS-INDEX:START -->' AGENTS.md)" -eq 1 && test "$(grep -c '<!-- ABEL:AGENTS-INDEX:END -->' AGENTS.md)" -eq 1 && test "$(grep -c '<!-- ABEL:AGENTS-INDEX:START -->' packages/pi-abel-workflow/AGENTS.md)" -eq 1 && test "$(grep -c '<!-- ABEL:AGENTS-INDEX:END -->' packages/pi-abel-workflow/AGENTS.md)" -eq 1 && rg -n 'pi-abel-workflow|@abel/pi-abel-workflow|prompts|skills|Affected suite|Full workspace suite' AGENTS.md packages/pi-abel-workflow/AGENTS.md`; run from `pi-packages/` and compare the diff with the recorded pre-task AGENTS hashes to prove edits are limited to the root managed block plus the new package index.

- [x] 1.2 Implement the four prompt entry points and verify real Pi expansion.
  - **Task ID / dependencies:** `1.2`; depends on `1.1`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Four stable workflow prompt entry points/Prompt frontmatter is valid`; `specs/abel-workflow-prompt-package/spec.md#Four stable workflow prompt entry points/Complete arguments are expanded`; `specs/abel-workflow-prompt-package/spec.md#Four stable workflow prompt entry points/Nested Markdown is not a prompt`; `specs/abel-workflow-prompt-package/spec.md#Four stable workflow prompt entry points/Missing required input`.
  - **Verification type:** `example`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/prompts.test.mjs`.
  - **Expected Red:** The four prompt files and their frontmatter, `<abel-request>` boundaries, required-input rules, and real Pi discovery/expansion behavior are absent, so the prompt assertions fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** Pi 0.84.1's real resource loader discovers exactly `abel-init`, `abel-design`, `abel-implement`, and `abel-diagnose`; each has a string `argument-hint`, includes the complete `$ARGUMENTS` expansion inside `<abel-request>`, preserves quoted and space-containing arguments through the real Pi prompt-template implementation, does not discover temporary nested Markdown, and closes on absent or ambiguous required input.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/prompts/abel-init.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-design.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-implement.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-diagnose.md`; `pi-packages/packages/pi-abel-workflow/test/prompts.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because four public prompt entrypoints and their test route become concrete. `pi-packages/AGENTS.md` is `none` because its package-boundary route from `1.1` remains accurate.
  - **AGENTS validation:** `git diff --check -- packages/pi-abel-workflow/AGENTS.md && rg -n 'prompts/|abel-init|abel-design|abel-implement|abel-diagnose|prompts.test.mjs' packages/pi-abel-workflow/AGENTS.md`; compare against the pre-task package-index hash and verify only the managed block changed.

- [x] 1.3 Implement the shared workflow Skill and Init contract.
  - **Task ID / dependencies:** `1.3`; depends on `1.2`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Self-contained shared workflow contract/No global Abel context exists`; `specs/abel-workflow-prompt-package/spec.md#Self-contained shared workflow contract/Gate is not a permission prompt`; `specs/abel-workflow-prompt-package/spec.md#Self-contained shared workflow contract/Shared rules are maintained once`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Core workflow skill is unavailable`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Research skills are available`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Research skill is missing`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Non-init skills are absent`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Default project path`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/OpenSpec is absent with Bun available`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Bun is unavailable and npm is available`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/OpenSpec installation fails`; `specs/abel-workflow-prompt-package/spec.md#Initialization behavior/Existing AGENTS content is present`.
  - **Verification type:** `static`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/workflow-skill.test.mjs`.
  - **Expected Red:** The authoritative `abel-workflow` Skill and the complete Init clauses do not exist, so static contract and independent-context assertions fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** The bundled shared Skill is sufficient without a global Abel `AGENTS.md`, defines Gate behavior without permission authorization, centralizes all cross-stage rules, and makes Init load the core Skill before writes, report research Skill paths or actionable absence, ignore non-Init Skills, select one Bun-or-npm toolchain, install/recheck OpenSpec safely, preserve unrelated and human AGENTS content, edit only managed blocks, and never edit `openspec/AGENTS.md`.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/skills/abel-workflow/SKILL.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-init.md`; `pi-packages/packages/pi-abel-workflow/test/workflow-skill.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because the shared Skill becomes the workflow-rule entrypoint. The root index is `none` because no cross-package boundary changes.
  - **AGENTS validation:** `git diff --check -- packages/pi-abel-workflow/AGENTS.md && rg -n 'abel-workflow|SKILL.md|shared|Init|AGENTS' packages/pi-abel-workflow/AGENTS.md`; compare the managed block with its pre-task hash and verify all other index text is preserved.

- [x] 1.4 Complete Design, Implement, Diagnose, and external browser-E2E stage contracts.
  - **Task ID / dependencies:** `1.4`; depends on `1.3`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/New design reaches Gate A`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Existing change is resumed`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Artifact integrity is invalid`; `specs/abel-workflow-prompt-package/spec.md#Design behavior and trusted delivery/Design is complete`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Valid cross-context handoff`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Task Red fails for the wrong reason`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Root cause is unverified`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Regression-first repair`; `specs/abel-workflow-prompt-package/spec.md#Diagnosis behavior/Requested fix changes behavior`; `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Browser E2E is not required`; `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Browser E2E is required but unavailable`.
  - **Verification type:** `static`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/stage-contracts.test.mjs`.
  - **Expected Red:** The three stage prompts and shared Skill do not yet encode complete Gate delivery, handoff, baseline, Red attribution, regression-first, Design-return, and external browser-E2E clauses, so the contract assertions fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** The package instructions enforce read-only Design boundaries, distinct Gate receipts and hashes, fresh-context Implement validation, target/affected/full baselines, exact Red attribution, Red-Green-Refactor, stable AGENTS checkpoints, evidence-first Diagnose, return to Design for new behavior or architecture, and `dev-browser` blocking only an approved browser-E2E task; no runtime agent registration, archive, publish, or implicit commit behavior is added.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/prompts/abel-design.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-implement.md`; `pi-packages/packages/pi-abel-workflow/prompts/abel-diagnose.md`; `pi-packages/packages/pi-abel-workflow/skills/abel-workflow/SKILL.md`; `pi-packages/packages/pi-abel-workflow/test/stage-contracts.test.mjs`.
  - **AGENTS impact:** `none` for both package and root indexes because the task only completes behavior within entrypoints already routed by `1.2` and `1.3`; no module, public/configuration surface, command, ownership, dependency boundary, or indexed invariant changes.
  - **AGENTS validation:** `git diff --quiet -- AGENTS.md packages/pi-abel-workflow/AGENTS.md`; if false, stop because the approved `none` contract has been violated.

- [x] 1.5 Make exact affected-suite commands the Implement dynamic repair boundary.
  - **Task ID / dependencies:** `1.5`; depends on `1.4`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected-suite baseline is green`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Existing affected failure is present`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Later run reveals a previously masked failure`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected failure is environmental`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected repair requires a substantive decision`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Full-suite-only baseline failure exists`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Implementation completes`.
  - **Verification type:** `static`.
  - **Red command:** `bun run --filter @abel/pi-abel-workflow test -- test/stage-contracts.test.mjs`.
  - **Expected Red:** Add only static assertions requiring the Implement prompt and shared Skill to state that exact affected-suite commands define a dynamic cross-module repair boundary, pre-existing failures remain separate from task Red and require verified root cause plus minimum repair, later failures receive three-way attribution, environmental failures block with an executable recovery condition and no speculative edit, substantive decisions return to Design, full-suite-only baseline failures stay outside automatic scope, and affected completion is all-green. The assertions fail because the current contracts omit these clauses; a missing test, invalid command, or unrelated product failure is not the contractual Red.
  - **Green behavior:** `abel-implement` and the authoritative shared Skill apply the approved dynamic boundary only to Implement, keep normalized failure evidence in context/report rather than a state file, permit additional repair paths only for attributed failures in that boundary, stop on unresolved or environmental failures, return substantive decisions to Design, and complete only with all target and affected verifications green plus no new full-suite failure.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test && bun run fallow:dead-code`.
  - **Full suite:** `bun run check && bun run --filter @gotgenes/pi-subagents verify:public-types && bun run --filter @gotgenes/pi-permission-system verify:public-types && bun run lint && bun run test && bun run --filter @gotgenes/pi-autoformat test:acceptance && bun run fallow:dead-code`; no new failure relative to the recorded baseline is permitted.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/test/stage-contracts.test.mjs`; `pi-packages/packages/pi-abel-workflow/prompts/abel-implement.md`; `pi-packages/packages/pi-abel-workflow/skills/abel-workflow/SKILL.md`. The static target list is the expected write scope; only a reproducible pre-existing failure exposed by the exact affected-suite commands may dynamically authorize an additional minimum-repair path.
  - **AGENTS impact:** `none` for `pi-packages/packages/pi-abel-workflow/AGENTS.md`, `pi-packages/AGENTS.md`, and the outer `AGENTS.md`: all already route the Implement prompt, shared Skill, static test, affected suite, and full suite, and this task changes no module, entrypoint, public/configuration surface, command, ownership, dependency/data-flow boundary, or indexed invariant. `openspec/AGENTS.md` remains forbidden and absent.
  - **AGENTS validation:** From `pi-packages/`, record `sha256sum AGENTS.md packages/pi-abel-workflow/AGENTS.md ../AGENTS.md` before writes; after Green require the same hashes and `test ! -e ../openspec/AGENTS.md`. Report all three indexes `unchanged` with these hashes as evidence.

## 2. Bundled research and commit Skills

- [x] 2.1 Implement whole-file configuration, shared HTTP reliability, and the Context7 Skill.
  - **Task ID / dependencies:** `2.1`; depends on `1.1`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Bundled skills are discovered`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Global deployment files are absent`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Tarball is inspected`.
  - **Verification type:** `property`.
  - **Properties / falsification:** A project configuration file always wins as a whole file regardless of user-file mutations; no value crosses files; one HTTP operation makes at most three attempts; non-retryable responses make exactly one attempt; retry waits obey the one-second, two-second, and capped `Retry-After` boundaries. Generate combinations of file existence, missing/unknown keys, quoted literal values, retryable/non-retryable statuses, network errors, and Retry-After forms with injected `cwd`, `home`, `fetch`, and `sleep`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/config-context7.test.mjs`.
  - **Expected Red:** The selected-file loader, literal parser, shared HTTP helper, Context7 CLI, Skill instructions, and configuration example are absent, so precedence, request, retry, stdin, response-shape, and redaction properties fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** The package selects project then user `.env` without merge or ambient `process.env`, parses values without expansion, exposes sanitized path-aware errors, supplies `context7.mjs search|context` with optional key and default/override API base, handles a lone stdin `-`, uses native Node.js 22 requests under the approved retry contract, and never reads the package installation directory or current global Skill deployment.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/skills/_shared/load-config.mjs`; `pi-packages/packages/pi-abel-workflow/skills/_shared/http-client.mjs`; `pi-packages/packages/pi-abel-workflow/skills/context7-auto-research/SKILL.md`; `pi-packages/packages/pi-abel-workflow/skills/context7-auto-research/context7.mjs`; `pi-packages/packages/pi-abel-workflow/config/.env.example`; `pi-packages/packages/pi-abel-workflow/test/config-context7.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because configuration selection, the shared HTTP boundary, and the Context7 runtime entrypoint become implementation routes. The root index is `none` because these remain package-internal boundaries.
  - **AGENTS validation:** `git diff --check -- packages/pi-abel-workflow/AGENTS.md && rg -n '_shared|load-config|http-client|context7|config/.env.example' packages/pi-abel-workflow/AGENTS.md`; compare against the pre-task hash and verify only its managed block changed.

- [x] 2.2 Implement the Grok-compatible and optional Tavily Skill.
  - **Task ID / dependencies:** `2.2`; depends on `2.1`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Bundled skills are discovered`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Global deployment files are absent`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Tarball is inspected`.
  - **Verification type:** `property`.
  - **Properties / falsification:** Accepted integer arguments round-trip without clipping and invalid safe-integer or documented-boundary values fail before fetch; merged sources contain unique URLs in first-occurrence order; explicit extra-source requests never report partial success; all provider operations preserve the three-attempt and secret-redaction invariants. Generate option boundaries, provider result orders and duplicates, retry outcomes, malformed JSON, invalid shapes, disabled/incomplete Tavily configurations, and representative secrets against fake `fetch` and `sleep`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/grok-search.test.mjs`.
  - **Expected Red:** The Grok/Tavily CLI, request and response adapters, enablement gate, fallback, source merge, option validation, sanitized config output, and tests are absent, so the provider and boundary properties fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** `web_search`, `web_fetch`, `web_map`, and `get_config_info` use a non-streaming OpenAI-compatible Chat Completions boundary and optional Bearer-authenticated Tavily `/search`, `/extract`, and `/map`; default model and URLs match the design; Tavily defaults off; fetch falls back to Grok after a sanitized Tavily warning; disabled map/extra-source operations close; search output is normalized and strictly validated; and no secret or full auth header is emitted.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/skills/grok-search/SKILL.md`; `pi-packages/packages/pi-abel-workflow/skills/grok-search/grok-search.mjs`; `pi-packages/packages/pi-abel-workflow/skills/_shared/http-client.mjs`; `pi-packages/packages/pi-abel-workflow/config/.env.example`; `pi-packages/packages/pi-abel-workflow/test/grok-search.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because the Grok/Tavily runtime entrypoint and provider data-flow boundary become concrete. The root index is `none` because no cross-package route changes.
  - **AGENTS validation:** `git diff --check -- packages/pi-abel-workflow/AGENTS.md && rg -n 'grok-search|Grok|Tavily|web_search|web_fetch|web_map' packages/pi-abel-workflow/AGENTS.md`; compare with the pre-task hash and verify the update is confined to the managed block.

- [x] 2.3 Implement the explicit-request, single-logical-change Git commit Skill.
  - **Task ID / dependencies:** `2.3`; depends on `2.2`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Bundled skills are discovered`; `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Commit skill is present without a commit request`.
  - **Verification type:** `static`.
  - **Red command:** `bunx --no-install vitest run packages/pi-abel-workflow/test/git-commit.test.mjs`.
  - **Expected Red:** The bundled Skill and the approved explicit activation, audit, exact-path staging, ambiguity stop, prohibited-command, and hook-failure clauses are absent, so static safety assertions fail; a missing test file or "no test files" result is not the contractual Red.
  - **Green behavior:** The Skill never commits from workflow completion or Gate approval; after an explicit request it reads applicable AGENTS files and all staged/unstaged status, excludes secrets/state/unrelated files, stages only `git add -- <explicit paths>` for one uniquely safe logical group, generates a repository-compliant Conventional Commit, stops without staging for ambiguity, prohibits `git add .`, `git add -A`, `git add -p`, `--no-verify`, `--amend`, reset, force push, and config mutation, and preserves staged state while reporting hook failures.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test`.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/skills/git-commit/SKILL.md`; `pi-packages/packages/pi-abel-workflow/test/git-commit.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because the fourth Skill and its safety responsibility become concrete. The root index is `none` because the package route already covers its bundled Skills.
  - **AGENTS validation:** `git diff --check -- packages/pi-abel-workflow/AGENTS.md && rg -n 'git-commit|explicit|commit|staging' packages/pi-abel-workflow/AGENTS.md`; compare with the pre-task hash and verify the change is limited to the managed block.

## 3. Distribution and final validation

- [x] 3.1 Verify the real tarball, independent user/project loading, and first-phase scope.
  - **Task ID / dependencies:** `3.1`; depends on `1.5`, `2.1`, `2.2`, and `2.3`.
  - **Requirements / scenarios:** `specs/abel-workflow-prompt-package/spec.md#Independently installable workflow package/User-scoped npm installation`; `specs/abel-workflow-prompt-package/spec.md#Independently installable workflow package/Project-scoped npm installation`; `specs/abel-workflow-prompt-package/spec.md#Bundled and external skills/Bundled skills are discovered`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Tarball is inspected`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Global deployment files are absent`; `specs/abel-workflow-prompt-package/spec.md#Safe package contents and independence/Forbidden workflow checkout is absent`; `specs/abel-workflow-prompt-package/spec.md#First-phase scope boundary and roadmap/First-phase package is inspected`.
  - **Verification type:** `E2E`.
  - **Red command:** From `pi-packages/packages/pi-abel-workflow/`, add only one static contract assertion to `test/distribution.test.mjs`, then run `bunx --no-install vitest run test/distribution.test.mjs`. The assertion SHALL read `AGENTS.md` and require it to state all of the following exact validation routes:
    - Target command: `bun run --filter @abel/pi-abel-workflow test -- test/distribution.test.mjs`.
    - Real pack route: `bun pm pack --destination <tmp>`, explicitly run from the package directory.
    - Affected suite: `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test && bun run fallow:dead-code`.
    - Full workspace suite: `bun run check && bun run --filter @gotgenes/pi-subagents verify:public-types && bun run --filter @gotgenes/pi-permission-system verify:public-types && bun run lint && bun run test && bun run --filter @gotgenes/pi-autoformat test:acceptance && bun run fallow:dead-code`.
  - **Expected Red:** The existing real tarball, Pi loader, user/project scope, and first-phase assertions pass, but the newly added static AGENTS contract assertion fails because `packages/pi-abel-workflow/AGENTS.md` currently provides only a generic target route, omits `fallow:dead-code` from its affected suite, and delegates the full suite to its parent instead of recording the exact executable command. A failure caused by host Skill discovery, missing test files, skipped pack/load execution, tarball contents, loader discovery, or another product behavior is not the contractual Red and must stop implementation.
  - **Green behavior:** Update only the managed block in `packages/pi-abel-workflow/AGENTS.md` so it records the exact distribution target, package-directory pack route, affected suite, and full workspace suite above. Re-run the same `bunx --no-install vitest run test/distribution.test.mjs` command and require the real pack, loader, scope, first-phase, and new AGENTS route assertions all to pass.
  - **Affected suite:** `bun run --filter @abel/pi-abel-workflow check && bun run --filter @abel/pi-abel-workflow lint && bun run --filter @abel/pi-abel-workflow test && bun run fallow:dead-code`.
  - **Full suite:** Compare with the recorded pre-write baseline, then run `bun run check && bun run --filter @gotgenes/pi-subagents verify:public-types && bun run --filter @gotgenes/pi-permission-system verify:public-types && bun run lint && bun run test && bun run --filter @gotgenes/pi-autoformat test:acceptance && bun run fallow:dead-code`; no new failure relative to baseline is permitted.
  - **Target files:** `pi-packages/packages/pi-abel-workflow/test/distribution.test.mjs`; `pi-packages/packages/pi-abel-workflow/AGENTS.md`.
  - **Scope restriction:** The Red phase may add only the static AGENTS route assertion to `test/distribution.test.mjs`. The Green phase may update only the managed block in `packages/pi-abel-workflow/AGENTS.md`. Do not change package behavior, provider implementations, manifest, tarball contents, installation semantics, first-phase scope, or any runtime resource completed by tasks `1.1` through `2.3`.
  - **AGENTS impact:** `update-existing` for `pi-packages/packages/pi-abel-workflow/AGENTS.md` because its final executable target, pack, affected-suite, and full-suite routes are currently incomplete. `pi-packages/AGENTS.md` is `none` because its package boundary and full-suite route remain accurate. The outer `/home/abelxiaoxing/work/pi-abelpackages/AGENTS.md` is `none` because it continues to route workspace work to the nested repository. `openspec/AGENTS.md` remains forbidden and is currently absent.
  - **AGENTS validation:** From `pi-packages/`, run `git diff --check -- packages/pi-abel-workflow/AGENTS.md && bun run --filter @abel/pi-abel-workflow test -- test/distribution.test.mjs`; compare `pi-packages/AGENTS.md` and the outer `AGENTS.md` with their pre-write SHA-256 baselines, confirm `../openspec/AGENTS.md` remains absent, and verify that only the package index managed block changed. Report `packages/pi-abel-workflow/AGENTS.md = updated`, `pi-packages/AGENTS.md = unchanged`, and outer `AGENTS.md = unchanged` before running the final affected and full suites.
