## 1. Trusted candidate admission

- [x] 1.1 Strictly consume complete ordinary unified diffs before structural retention. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Candidate artifact passes structural submission but cannot load`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Worker diff exceeds its result boundary`. **Direct prerequisites:** `[]`. **Dependency type/output/reason:** root task; produces strict ordinary-diff admission used by Task 1.2 and every later candidate. **Non-task preconditions:** verify both design harness hashes/capabilities; bootstrap capability includes semantic-contract delivery, classified termination, requested thinking `low`, both public-clamp policy controls, one shared schema/strict/no-submit pre-acceptance repair token, `maxSubmitCallsPerLaunch=2`, and zero real Provider requests; compat extension and package Agent are regular files whose per-run SHA-256 values are session evidence; record target/affected/full baselines; confirm only the original change root is dirty; bind real `Bound` snapshots; Revision-E Task 1.1 identity and launches remain historical exhausted evidence; new Revision-F SDK metadata uses task identity `preserve-child-provider-payload-compat-1.1-f`, `launchIndex=0`, `correctionIndex=0`, immutable system-prompt/contract/write-set/verification/snapshot and provider-model-requested-effective-supported digests, and null predecessor/rejection digests. **Dispatch context:** Gate A receipt; proposal; both delta specs; design Decisions 3, 5, and 7; root `AGENTS.md`; `src/contracts.ts`, `src/submit-tool.ts`, `src/patch.ts`, `src/result-store.ts`, `src/file-snapshot.ts`, `agents/implementation-worker.md`, `test/contracts.property.test.ts`, `test/patch.integration.test.ts`, and `test/text-patch-headers.integration.test.ts`. **Suggested wave / parallel eligibility:** wave 1, `serial`; root of a strict five-node DAG. **Planned write set:** Red only new `test/candidate-diff-admission.property.test.ts` plus `test/contracts.property.test.ts`, where the latter changes exactly two pre-existing controls: change the multi-file control to `@@ -1 +1,2 @@` with ` old`/`+new`, then `@@ -5,2 +5,2 @@` with its two context lines and a final empty array element for LF; change only `validDiffResult()` hunk `@@ -1,2 +1,3 @@` to `@@ -1,2 +1,2 @@`. Green only `src/contracts.ts`; `src/submit-tool.ts` and `src/patch.ts` are validation-only because they already call `validateDiffResult()`/`diffWritePaths()`, and any code write there requires Design. Refactor subset only. `AGENTS.md` and OpenSpec files are parent-only. **Candidate delivery/admission:** use both hash-bound harnesses from design Decision 7. SDK bootstrap: exactly five tools, in-memory session/settings, all retry disabled, requested thinking fixed to `low`, effective and ordered supported levels computed with Pi public `clampThinkingLevel()`/`getSupportedThinkingLevels()`, the exact hash-bound semantic contract appended to the package Agent system prompt, write-set/verification/real `Bound` plus provider-model-requested-effective-supported digests bound, model metadata independently recomputed before auth/Provider access with drift failing closed, one stable tool-facing `id=taskId=preserve-child-provider-payload-compat-1.1-f`, observed-read `Bound` returned, and parent-only attempt metadata. Public tool/turn events classify TypeBox schema error, identity/product/reference strict error, ordinary no-submit stop/length, Provider error/abort, and exhaustion. Schema/strict/no-submit faults share one pre-acceptance repair token; no-submit may use one fixed public same-run `Agent.followUp()`, Provider error/abort never does, and the next fault terminates `bootstrap-pre-acceptance-repair-exhausted`. A phase still has at most two process launches total, where launch 1 is either transport redispatch or semantic correction, never both. The reference harness independently admits identical Red bytes in two fresh checkouts, each after `bun run check`: `test/contracts.property.test.ts` as expected-green and the dedicated target as expected-red with its approved identity. Both must classify correctly before main application. Green reruns each target as expected-green over identical candidate bytes in separate fresh-checkout transactions and must conform to the strict parser. Failed Revision-D outputs and Revision-E Task 1.1 attempts are never supplied. **Conflict/resources/locks:** conflicts with `[1.2, 2.1, 3.1, 4.1]` through admission order; exclusive `candidate-diff-admission`, `main-apply`, and `vitest-runtime-provider`; verification lock `candidate-diff-admission-suite`. **Verification type:** `property`. **Red precheck:** after parent application, run `bun run check && bun run test:target test/contracts.property.test.ts`; both must be Green before target-Red classification, otherwise reject the artifact. **Red command:** `bun run test:target test/candidate-diff-admission.property.test.ts`. **Expected Red:** both Red files load; the two normalized existing controls remain Green against current code; the dedicated target fails only because header-only admission accepts at least one impossible hunk or unconsumed suffix; valid add/modify/delete controls pass fixture setup. Parse/import/no-test/wrong-fixture or existing-control failure is artifact rejection, not Red. **Green behavior:** `src/contracts.ts` complete ordinary-diff consumption rejects hunk underflow/overflow, missing final LF, fence/sentinel/other tail, forbidden binary/copy/rename/mode/submodule forms, duplicate/escaping/noncanonical targets, and over-limit results before real submit retention; valid ordinary add/modify/delete and both normalized existing controls remain accepted; unchanged `createSubmitTool()` and `applyRetainedPatch()` demonstrably reuse that parser before retention/Git applicability screening. **Affected suite / validation:** `bun run check`; `bun run test:target test/candidate-diff-admission.property.test.ts test/contracts.property.test.ts test/patch.integration.test.ts test/text-patch-headers.integration.test.ts test/child-session.integration.test.ts`; stable checkpoint `bun run verify` and `bun run check:agents`. **AGENTS impact:** `update-existing` root only after Green: route strict candidate-diff admission in `src/contracts.ts` and defensive parent apply in `src/patch.ts`; evidence is the complete task diff plus `bun run check:agents`.

- [x] 1.2 Preflight the exact retained candidate in an isolated sibling checkout and apply only admitted identical bytes. **Requirement/Scenario ownership:** `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Worker delivers a task phase`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Candidate artifact cannot load or has the wrong Red identity`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Task Red fails for the wrong reason`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Task Red contract is invalid`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Valid cross-context handoff`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Compatible tasks produce parallel results`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Unrelated sibling application preserves currency`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Related file change makes a result stale`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected-suite baseline is green`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Existing affected failure is present`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Later run reveals a previously masked failure`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected failure is environmental`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Full-suite-only baseline failure exists`. **Direct prerequisites:** `[1.1]`. **Dependency type/output/reason:** `artifact` and `contract` on 1.1 for strict retained bytes and parser conformance; produces the isolated preflight/apply transaction required by all later tasks. **Non-task preconditions:** Task 1.1 target/affected/checkpoint Green and strict parser/reference conformance accepted; both harness hashes and capabilities still match; reference harness reports bwrap/strictDiff ready with zero real Provider requests; bootstrap requests literal `low`, publicly clamps against the selected model, binds ordered requested/effective/supported policy with provider/model plus write set/verification/current snapshot and observed-read `Bound`, independently rejects metadata drift before Provider access, delivers the hash-bound semantic contract in the Agent system prompt, classifies public termination, and shares one pre-acceptance repair token across schema/strict/no-submit faults; fresh real `Bound`, typed baseline, verification, package/lock/dependency snapshots; record target/affected/full baselines. **Dispatch context:** Gate A receipt; design Decisions 4, 5, and 7; Task 1.1 accepted output; root `AGENTS.md`; `src/contracts.ts`, `src/file-snapshot.ts`, `src/result-store.ts`, `src/patch.ts`, `src/runtime.ts`, `src/submit-tool.ts`, `src/scoped-tools.ts`, package scripts, existing patch/concurrency/runtime/lifecycle tests, and the fixed `CandidatePreflightInput/Result/Dependencies` API in design. **Suggested wave / parallel eligibility:** wave 2, `serial`; consumes 1.1 and owns parent apply. **Planned write set:** Red only new `test/candidate-preflight.property.test.ts`; Green only new `src/candidate-preflight.ts`, `src/contracts.ts`, `src/result-store.ts`, `src/patch.ts`, `src/runtime.ts`, `src/submit-tool.ts`, plus exact distribution-member updates `scripts/pack-check.mjs` and `test/distribution.test.mjs`; Refactor subset only. **Candidate delivery/admission:** repaired Revision-F SDK bootstrap produces candidates under requested-`low`/public-clamped effective thinking with bound supported levels and fail-closed metadata drift, delivered semantic contracts, classified termination, and the one shared pre-acceptance repair token; the hash-bound reference harness independently admits both Red and Green. Red uses a non-literal dynamic import and exact seam `not_ready: isolated candidate preflight is not implemented`. Green must pass reference/product conformance before both extracted harness files are deleted; product strict/preflight admission is mandatory afterward. Parent alone applies exact admitted bytes. **Conflict/resources/locks:** conflicts with `[2.1, 3.1, 4.1]`; exclusive `candidate-preflight`, `main-apply`, `bwrap-namespace`, `temporary-checkout`, `vitest-runtime-provider`; verification lock `candidate-preflight-suite`. **Verification type:** `property`. **Red command:** `bun run test:target test/candidate-preflight.property.test.ts`. **Expected Red:** at least eight tests discover and fail only with `not_ready: isolated candidate preflight is not implemented`; fixtures use the fixed API and real `snapshotFiles()`/`mergeBounds()` rather than invented flags or digest strings. **Green behavior:** strict/write-set/currentness checks; non-local/non-hardlink detached clone with no alternates; typed reconstruction of approved files/deletions/modes; exact candidate application; required Bubblewrap with unshared network, tmpfs `/tmp`, checkout-only repository write, read-only dependencies, no host/home/credentials; argv-only `bun run check` then exact target; normalized classification/identity; deterministic cleanup on success/failure/cancel; host/dependency/main snapshot immutability; reference/product conformance; serialized parent-only application of identical bytes. **Affected suite / validation:** `bun run check`; `bun run test:target test/candidate-preflight.property.test.ts test/candidate-diff-admission.property.test.ts test/contracts.property.test.ts test/patch.integration.test.ts test/text-patch-headers.integration.test.ts test/file-concurrency.integration.test.ts test/runtime-scheduler.integration.test.ts test/lifecycle.integration.test.ts test/stage-contracts.test.mjs`; stable checkpoint `bun run verify`, `bun run pack:check`, and `bun run check:agents`. **AGENTS impact:** `update-existing` root: route `src/candidate-preflight.ts`, typed retained verification/baseline state, isolated parent preflight, and parent-only serialized apply; validate actual paths/commands with `bun run check:agents`.

## 2. Parent payload bridge core

- [x] 2.1 Implement generation/model-bound Provider capture, delegate unwrapping, callback FIFO composition, and final Responses normalization as a pure package module. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Parent payload compatibility rewrites a child request`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Parent payload compatibility cannot complete`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Pi contains an internal parent handler error`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/OpenAI Responses child request has no optional output cap`. **Direct prerequisites:** `[1.2]`. **Dependency type/output/reason:** `contract` on 1.2 for product candidate preflight; produces a pure bridge API consumed by lifecycle wiring. **Non-task preconditions:** 1.2 target/affected/checkpoint Green, extracted harnesses retired, product strict/preflight admission active, fresh snapshots and baselines. **Dispatch context:** Gate A; design Decisions 1–2, 5, and 7; `src/parent-provider.ts`, Provider/request option types, Pi public registration semantics, existing child/provider tests. **Suggested wave / parallel eligibility:** wave 3, `serial`; bridge core follows admission and feeds 3.1. **Planned write set:** Red only new `test/parent-payload-bridge.property.test.ts`; Green only new `src/parent-payload-bridge.ts`, `src/parent-provider.ts`, `scripts/pack-check.mjs`, and `test/distribution.test.mjs`; the two distribution files add exactly `package/src/parent-payload-bridge.ts` after Task 1.2's already accepted `candidate-preflight.ts` member and make no unrelated member change; Refactor subset only. **Candidate delivery:** repaired Revision-F SDK bootstrap with requested `low`, public-clamped effective/bound supported thinking levels, fail-closed model-policy drift, delivered semantic contract, classified termination, and one shared pre-acceptance schema/strict/no-submit repair token; product `src/candidate-preflight.ts` is the sole admission path; same phase has the global maximum-two-launch state machine. **Conflict/resources/locks:** conflicts with `[3.1, 4.1]`; exclusive `parent-payload-callback`, `parent-provider`, `vitest-runtime-provider`; verification lock `provider-payload-suite`. **Verification type:** `property`. **Red command:** `bun run test:target test/parent-payload-bridge.property.test.ts`. **Expected Red:** optional dynamic import loads at least eight fixed-seed tests and all fail only with `not_ready: parent payload bridge is not implemented`. **Green behavior:** exact generation/session/model readiness, package-only recursive unwrapping, successful-stream-only capture, one original delegate call, child→parent→safe-object→Responses-delete order, undefined identity semantics, non-mutation, missing/stale/rejected/unsafe fail-closed, non-Responses preservation, serialized concurrent callback entry, cancellation checks, and Pi-hidden handler result preservation. **Affected suite / validation:** `bun run check`; `bun run test:target test/parent-payload-bridge.property.test.ts test/child-session.integration.test.ts test/child-session-cancellation.test.ts test/runtime-scheduler.integration.test.ts test/distribution.test.mjs`; `bun run pack:check`; stable checkpoint `bun run verify` and `bun run check:agents`. **AGENTS impact:** `none`; the pure module is not yet an entrypoint route, verified by the complete task diff and unchanged index claims.

## 3. Parent lifecycle and child runtime wiring

- [x] 3.1 Install the wrapper through public session/model hooks, pass ready captures into child phase Providers, clear bridge state with Runtime lifecycle, and verify the installed Responses wire path. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Child session is created`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Runtime bound is reached`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Phase times out`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Dispatcher returns nested usage`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Pi lifecycle ends the stage`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Filesystem is inspected after delegation`. **Direct prerequisites:** `[2.1]`. **Dependency type/output/reason:** `artifact` and `contract` on 2.1 bridge API; produces the repaired product `abel_dispatch` route and real serializer evidence required by 4.1. **Non-task preconditions:** 2.1 Green; public `session_start`, `model_select`, `before_agent_start`, `session_shutdown`, and `ModelRegistry.registerProvider` available; fresh snapshots/baselines. **Dispatch context:** Gate A; design Decisions 1–2, 5, and 7; root `AGENTS.md`; `src/index.ts`, `src/runtime.ts`, `src/parent-provider.ts`, `src/parent-payload-bridge.ts`, `src/child-session.ts`, `src/empty-resource-loader.ts`; activation/lifecycle/prompt tests. **Suggested wave / parallel eligibility:** wave 4, `serial`; consumes bridge and repairs route. **Planned write set:** Red only new `test/parent-payload-lifecycle.integration.test.ts` and `test/openai-responses-child.integration.test.ts`; Green only `src/index.ts`, `src/runtime.ts`, `src/parent-provider.ts`, `src/parent-payload-bridge.ts`, and only if an invariant assertion is required `src/child-session.ts`; Refactor subset only. **Candidate delivery:** repaired Revision-F SDK bootstrap with requested `low`, public-clamped effective/bound supported thinking levels, fail-closed model-policy drift, delivered semantic contract, classified termination, one shared pre-acceptance schema/strict/no-submit repair token, and product preflight; after Green, a fresh product-route smoke probe must succeed before Task 4.1. **Conflict/resources/locks:** conflicts with `[4.1]`; exclusive `parent-provider-registration`, `parent-payload-callback`, `runtime-lifecycle`, `real-openai-responses-serializer`, `local-fetch-recorder`, `vitest-runtime-provider`; verification locks `provider-payload-suite` and `openai-responses-wire`. **Verification type:** `example`. **Red command:** `bun run test:target test/parent-payload-lifecycle.integration.test.ts test/openai-responses-child.integration.test.ts`. **Expected Red:** both protocol-valid modules load; lifecycle test fails on missing public wrapper capture and installed Responses wire test fails on the same route before external network. Syntax/import/no-test/protocol failures are artifact rejection. **Green behavior:** session start begins generation; model select/before-agent-start idempotently install one wrapper; successful parent request arms exact-model capture; child uses one original delegate with fresh auth and five tools/zero resources; injected local fetch records inherited request-specific instructions/input and no `max_output_tokens`; missing/stale/rejected/unsafe bridges send zero requests; replacement requires recapture; finish/reload/replacement/shutdown clear stale state; no callback/payload persists. **Affected suite / validation:** `bun run check`; `bun run test:target test/parent-payload-lifecycle.integration.test.ts test/openai-responses-child.integration.test.ts test/parent-payload-bridge.property.test.ts test/activation.test.ts test/prompt-activation.integration.test.ts test/child-session.integration.test.ts test/child-session-cancellation.test.ts test/runtime-scheduler.integration.test.ts test/lifecycle.integration.test.ts test/drain.property.test.ts`; stable checkpoint `bun run verify` and `bun run check:agents`. **AGENTS impact:** `update-existing` root: route session-owned capture, unwrapped delegation, lifecycle clearing, and phase composition; validate with `bun run check:agents`.

## 4. Finite recovery and workflow contract

- [x] 4.1 Complete task-level phase/correction identity, return typed sanitized recovery for transport/artifact/environment/design outcomes, and update the Abel Implement contract so artifact exhaustion has no automatic Design transition. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/First request fails within an unchanged contract`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Mechanical redispatch fails again`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Artifact correction budget is exhausted`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/Recovery would expand the contract`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/One parallel branch fails`; `specs/private-agent-orchestration/spec.md#Single mechanical redispatch and branch isolation/User cancels a batch`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Artifact correction budget is exhausted`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Affected repair requires a substantive decision`; `specs/abel-workflow-prompt-package/spec.md#Implementation behavior/Implementation completes`. **Direct prerequisites:** `[3.1]`. **Dependency type/output/reason:** `contract`, `artifact`, and `conflict-order` on 3.1 for a working product Provider route and accepted preceding phases; finalizes product identity/recovery/workflow semantics. **Non-task preconditions:** all prior targets/affected/checkpoints Green; exact prompt/Skill baselines recorded; a fresh repaired-route SDK host uses `SessionManager.inMemory()` and retry-disabled `SettingsManager.inMemory()`, explicitly loads only the working-tree package and approved compat extension, verifies package `abel-implement` prompt provenance, exposes only host `abel_dispatch`, and proves with faux responses that neither host nor product child emits auto-retry before a real Task 4.1 candidate request. **Dispatch context:** design Decisions 5–7; `src/runtime.ts`, `src/worker.ts`, `src/contracts.ts`, `src/submit-tool.ts`, `src/child-session.ts`, `prompts/abel-implement.md`, `skills/abel-workflow/SKILL.md`, stage/redispatch/runtime tests, root `AGENTS.md`. **Suggested wave / parallel eligibility:** wave 5, `serial`; final node. **Planned write set:** Red only new `test/runtime-recovery.property.test.ts` and existing `test/stage-contracts.test.mjs`; Green only `src/runtime.ts`, `src/worker.ts`, `src/contracts.ts`, `src/submit-tool.ts`, `src/child-session.ts`, `prompts/abel-implement.md`, and `skills/abel-workflow/SKILL.md`; Refactor subset only. **Candidate delivery:** every Task 4.1 candidate MUST use repaired product `abel_dispatch` in the fresh SDK host above; no CLI/global retry settings or bootstrap submit wrapper participates. The host Agent calls only `abel_dispatch`; the product creates the actual five-tool/zero-resource Worker. Successful structural delivery with stable `taskId` and distinct phase-local request id is route/identity end-to-end acceptance; parent product preflight remains the only admission. **Conflict/resources/locks:** exclusive `logical-worker-registry`, `runtime-recovery`, `workflow-contract`, `vitest-runtime-provider`; verification lock `runtime-recovery-suite`; conflict-order from every predecessor. **Verification type:** `property`. **Red command:** `bun run test:target test/runtime-recovery.property.test.ts test/stage-contracts.test.mjs`. **Expected Red:** tests load but fail because product identity remains request-coupled, recovery is generic, total transport/artifact launch use is not product-enforced, and workflow still sends every wrong Red to Design. **Green behavior:** `RequestEnvelope.taskId` is stable while phase request id/write set/snapshot/verification and predecessor correction binding are phase-local; `createSubmitTool` and `runChildSession` accept/validate separate task and request identities; WorkerRegistry is keyed by taskId; task-level equality permits phase-local writes; global launch index is capped at 1 so transport redispatch and artifact correction cannot stack; environment and substantive defects stay distinct; stable recovery preserves siblings and leaks no raw diagnostics; prompt/Skill route artifact rejection to bounded correction/blocking and reserve Design for contract changes; fresh SDK-host/faux evidence shows host and child retry are both disabled and then a real product-route candidate succeeds. **Affected suite / validation:** `bun run check`; `bun run test:target test/runtime-recovery.property.test.ts test/stage-contracts.test.mjs test/child-session.integration.test.ts test/file-concurrency.integration.test.ts test/runtime-scheduler.integration.test.ts test/workflow-routing.integration.test.ts test/runtime-activity.property.test.ts test/subagent-activity.integration.test.ts test/drain.property.test.ts`; then `bun run verify`, package-wide regression `bun run traceability:check`, the change-local 34/34 exactly-once Scenario audit defined by this design receipt, and `bun run check:agents`. **AGENTS impact:** `update-existing` root: route typed recovery and stable-task/phase-request boundaries; validate exact distribution members if shipped modules changed and run final index verification.
