## 1. Runtime lifecycle contract

- [x] 1.1 Implement request-scoped lifecycle observation and typed terminal failure outcomes. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Compact structured delivery/Structured evidence succeeds`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Complete diff delivery succeeds`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Result schema is invalid`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Complete result exceeds its limit`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Child session is created`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Runtime bound is reached`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Phase times out`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Dispatcher returns nested usage`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Invalid request is rejected`. **Direct prerequisites:** none. **Dependency type / wave / parallel eligibility:** no dependency; wave 1; serial because the task owns the Runtime/faux-Provider verification lock. **Verification type:** property. **Red command:** `bun run test:target test/runtime-activity.property.test.ts`. **Expected Red:** the new test file establishes valid-transition traces, invalid-request silence, typed timeout/cancellation, exactly-one-terminal, and observer non-interference; it fails because the private observer and failure kinds do not exist—a missing test file, no-test-files result, syntax error, or unrelated baseline failure is not the contractual Red. **Green behavior:** after strict validation Runtime emits `queued`, the Scheduler execute closure emits `running`, settlement emits exactly one typed terminal event, observer exceptions do not change the existing DispatchResult, and content/details/usage/resultId/retention semantics remain unchanged. **Affected suite:** `bun run test:target test/runtime-activity.property.test.ts test/runtime-scheduler.integration.test.ts test/child-session.integration.test.ts test/child-session-cancellation.test.ts test/scheduler.property.test.ts test/lifecycle.integration.test.ts test/two-worker.e2e.test.ts`. **Exact target files:** `src/child-session.ts`; `src/runtime.ts`; new `test/runtime-activity.property.test.ts`. **Conflict/resource/verification locks:** exclusive `src/runtime.ts`, `src/child-session.ts`, `vitest-runtime-provider`, temporary Git fixture directories, and faux Provider identity. **AGENTS impact:** `none`; evidence: all edits remain under the existing child-execution and Runtime/Scheduler ownership routes and add no module, entrypoint, command, configuration surface, or context boundary. **Checkpoint:** inspect the complete task diff, run the target after every edit and the affected suite after refactor, and record the `none` evidence in the implementation report.

## 2. Width-safe presentation module and package surface

- [x] 2.1 Add the TUI activity owner, bounded renderers/Widget, property tests, and direct Pi TUI package contract. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Compact structured delivery/Interactive run fails with an unsafe reason`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Successful result is expanded`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Multiple Subagents are active`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Activity display overflows`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/Display layer encounters an error`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/Private child data is inspected through the display`. **Direct prerequisites:** task 1.1. **Dependency type / wave / parallel eligibility:** artifact dependency on task 1.1 lifecycle/failure types; wave 2; serial because this task owns manifest, lockfile, package-list, and AGENTS writes. **Verification type:** property. **Red command:** `bun run test:target test/subagent-activity.property.test.ts test/package-contract.test.mjs test/distribution.test.mjs`. **Expected Red:** the tests load a real activity module and falsify ordering, line-width, 12-line bound, exact overflow, terminal cleanup, privacy-marker, direct peer/dev declaration, and tarball-member claims; they fail because the module, peer declaration, and shipped member do not exist—a missing test file, no-test-files result, syntax error, or unrelated baseline failure is not the contractual Red. **Green behavior:** one ActivityController owns one 100 ms timer; active rows are keyed by toolCallId and sorted by Runtime admission sequence; the two-line above-editor Widget is at most 12 lines and reports exact hidden running/queued counts; every line is width-safe; expanded output contains only approved evidence/diff summaries; hostile controls, paths, Provider/model identity, complete citations, and complete diffs are absent; `@earendil-works/pi-tui` is a `"*"` peer and the installed version is a dev dependency with no ordinary dependencies. **Affected suite:** `bun run check`; `bun run lint`; `bun run test:target test/subagent-activity.property.test.ts test/package-contract.test.mjs test/distribution.test.mjs`; `bun run pack:check`; `bun run check:agents`. **Exact target files:** new `src/subagent-activity.ts`; new `test/subagent-activity.property.test.ts`; `package.json`; `bun.lock`; `test/package-contract.test.mjs`; `test/distribution.test.mjs`; `scripts/pack-check.mjs`; managed block of root `AGENTS.md`. **Conflict/resource/verification locks:** exclusive `src/subagent-activity.ts`, `package.json`, `bun.lock`, both exact tarball-member lists, root AGENTS managed block, `package-manifest`, `bun-lock`, and `tarball-pack`. **AGENTS impact:** `update-existing` on root `AGENTS.md`; add the `src/subagent-activity.ts` presentation-owner route and its process-local active-state/non-TUI boundary, create no nested index, preserve human text, and validate marker/path/command integrity with `bun run check:agents`. **Checkpoint:** inspect the full task diff for only approved display/package/index effects, update the managed block only after Green/Refactor, run all affected commands, and report the changed index path and evidence.

## 3. Extension integration and compatibility

- [x] 3.1 Wire TUI-only lifecycle updates, terminal details, renderers, cleanup, and user documentation through the existing extension entrypoint. **Requirement/Scenario ownership:** `specs/private-agent-orchestration/spec.md#Compact structured delivery/Interactive run is visibly delegated`; `specs/private-agent-orchestration/spec.md#Compact structured delivery/Interactive run reaches a terminal state`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Pi lifecycle ends the stage`; `specs/private-agent-orchestration/spec.md#Ephemeral bounded runtime lifecycle/Filesystem is inspected after delegation`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/Interactive TUI receives activity presentation`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/TUI result is rendered again`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/Non-TUI request runs`; `specs/private-agent-orchestration/spec.md#TUI-only private activity compatibility/Public controls are inspected`. **Direct prerequisites:** tasks 1.1 and 2.1. **Dependency type / wave / parallel eligibility:** artifact dependencies on lifecycle and presentation modules; wave 3; serial because this is final integration and owns the full-suite verification lock. **Verification type:** example. **Red command:** `bun run test:target test/subagent-activity.integration.test.ts`. **Expected Red:** integration examples exercise Pi `renderCall`/`renderResult`, `onUpdate`, Widget/status calls, TUI-only `activityDisplay`, non-TUI structural equivalence, equal request-id isolation, invalid-envelope silence, redispatch terminal timing, and session cleanup; they fail because `src/index.ts` has no presentation wiring—a missing test file, no-test-files result, syntax error, or unrelated baseline failure is not the contractual Red. **Green behavior:** only `ctx.mode === "tui"` creates an observer/controller binding or appends `activityDisplay`; valid runs show queued/running/typed-terminal inline states and active Widget entries; terminal entries disappear immediately; session start/shutdown clears UI and timer around the existing drain; print, JSON, and RPC return their existing content/details/usage/resultId/errors/events unchanged; no command, setting, Fleet/viewer, or stop/resume/steer control is registered; README documents only the approved TUI presentation and privacy boundary. **Affected suite:** `bun run test:target test/subagent-activity.integration.test.ts test/subagent-activity.property.test.ts test/runtime-activity.property.test.ts test/activation.test.ts test/runtime-scheduler.integration.test.ts test/lifecycle.integration.test.ts test/child-session.integration.test.ts test/usage.property.test.ts test/package-contract.test.mjs test/distribution.test.mjs`; then `bun run verify`; `bun run traceability:check`; `bun run check:agents`. **Exact target files:** `src/index.ts`; new `test/subagent-activity.integration.test.ts`; `README.md`. **Conflict/resource/verification locks:** exclusive `src/index.ts`, `README.md`, `vitest-full-suite`, `tarball-pack`, and `traceability-check`. **AGENTS impact:** `none`; evidence: task 2.1 already indexes the new presentation owner and boundary, while this task only connects that owner to the already indexed `src/index.ts` entrypoint and adds no route, public/configuration surface, command, ownership change, or context boundary. **Checkpoint:** inspect the complete final implementation diff and root index, run target after each edit, run the affected and full suites after refactor, confirm no new failure against baseline, and record the `none` evidence.
